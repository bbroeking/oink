# Build 150 — 2026-07-14 (v1.3, iOS buildNumber 150)

> Supersedes build 149 (never delivered). The forcing event: knock-to-join
> went live in prod tonight (`20260742000000_join_requests`), and 149's
> join door predates the knock UI — its Join buttons would file requests
> with no "asked — waiting" state to show. 150 = everything in 149 plus
> the knock client, the founder's evening layout calls, and it ships
> against a database whose push delivery finally works.

## Since build 149

### Open Sounders are knocked on, never walked into (founder call)

- Join door rows: "ask to join ›" → "asked — waiting" + "take it back"
  (cancel); max 3 pending knocks with the hint "three doors knocked —
  wait for an answer, or take one back."
- Crew side (SounderCard): "{name} wants to dig with you" rows with
  "let them in" / "not now"; quiet crew-full note. Realtime on
  `crew_join_requests` keeps both sides fresh.
- Door blurb + guide step 1: "slip into" → "ask into". Invites keep
  instant accept (consent already flows both ways).
- Server (`20260742000000`, LIVE): `request_to_join` /
  `accept_join_request` (any member, cap-checked at accept) /
  `decline_join_request` (silent) / `cancel_join_request`; requests never
  reserve seats; `join_crew` is a transitional shim ({ok, requested:true})
  so pre-knock clients can't walk in either; `crew_state` carries
  `join_requests_in/out`. Harness: 10-scenario smoke green.

### Season scroll ends honestly

- `SeasonEndBeat` removed entirely (founder: "I don't really get what the
  point of this is") — the XP tease duplicated the track one glance up.
  Season 1 now ends on the "…and N more tiers ›" expander. Ready claims
  already live in the top ClaimAllBar (≥1) since 149.

### Referral recruits board: numbers mean one thing

- The top-recruiter circle now shows the COUNT (pigs brought in),
  matching the rows' right-hand numbers — it showed rank ("1") in the
  count position. Rank is told by the "top recruiter" kicker.

## Server-side context shipped tonight (not in the binary)

- `20260741000000` — push delivery FIXED: `send_push_to_user` called
  `extensions.http_post`, which never existed (pg_net lives in `net.*`);
  every server push since May silently failed. One-word fix; verified
  end-to-end (request_id 1 = the first push the DB ever queued).
- `20260741100000` — push sending locked to server-only (EXECUTE revoked
  from authenticated/anon/PUBLIC on `send_push_to_user` + `dev_send_push`;
  verified 42501 as a player). Triggers/cron are SECURITY DEFINER and
  unaffected.
- `20260742100000` — one-off founder snuffle to "I Love Brian"
  (request_id 2, has_token: t) — first real-device delivery test.
- New static test `__tests__/pushDelivery.test.ts` guards the `net.http_post`
  reference against future carried-def regressions.

## Known gaps at build time

- Demo reviewer account (demo@ticklethepig.com) was found DELETED and
  recreated blank with documented creds — needs re-seeding before the 1.3
  review submission.
- Two empty test crews in prod pending cleanup ("The Bristle Sniffers",
  "The Muddy Diggers").

## Verification

- `npx tsc --noEmit` clean; Jest 42 suites / 521 tests green.
- db-harness CHAIN green (join-requests smoke + full stack, revoke
  migration included).
- Live sim: ask-to-join door renders + fails soft pre-push; post-push the
  RPCs are live. Leaver nudge, spotlight (hole on the join door), rename
  settings row, banner dig footer, claim bar, receipt — all verified in
  149's pass and unchanged.

## Numbering

- CFBundleVersion 150 via EAS remote auto-increment. Marketing version
  1.3. **Deliver build-150.ipa only** — 147/148/149 are superseded;
  149's ipa is deleted from disk.
