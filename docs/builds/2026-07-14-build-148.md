# Build 148 — 2026-07-14 (v1.3, iOS buildNumber 148)

> The post-1.2-release lane: 1.2 went live on the App Store today; this build
> carries everything since build 142 — the sounder onboarding funnel, the
> season tab rebuilt around the five player questions, the full visual pass,
> and the camera-pass art sweep. All 15 staged migrations are already pushed
> to prod, so the client and DB land together.

## Sounder onboarding funnel (the crewless get a path)

- **`hooks/useSounderPath`** — derived state machine (hook → taste → join →
  first_dig → done), never stored flags; prompts every session until the
  player has tasted a dig, joined a Sounder, and dug with them once.
- **SounderStepCard** slots into the season tab as the single next-step CTA;
  **SounderHomeCard** gets the JoinDoor benefits list and the sounder gloss
  line ("a 'sounder' is the true old word for a herd of wild pigs…").
- Sounder creation auto-grants a random herd name — no name prompt mid-flow.

## Season tab, rebuilt around the five questions

- Loop-line banner → **WindowStrip** (when do I dig) → SounderStepCard (who
  with) → **YourTakeStrip** (what have I earned; pouch uses the golden-truffle
  art, never the premium crest) → **RaceSection** (how's the herd doing) →
  collapsed pass track + **SeasonEndBeat** (when does it end).
- Guide auto-open removed (`GUIDE_EVERY_VISIT` now dev-only); ClaimAllBar;
  premium banner tells the Slop Club truth.
- **Race ledger departed rows**: a leaver's finds stay with the crew, row
  reads "N this week · trotted on" at 0.55 opacity, sorted after members
  (server: `race_crew_detail` carries `departed`).

## Social friction fixes

- **Tickled-out split**: global 3h budget ("all tickled out — your snout
  needs a rest") vs per-pair 24h tag ("tickled today") — day-rhythm copy,
  no countdowns.
- **Friend favorites**: star a friend (WHIMSY.sun), favorites sort to top
  (RLS owner-only table).
- **Invite seats**: members + pending ≤ 4 everywhere; pending invites show
  "take it back" cancel; picker disables at cap with a "sounder full" hint.
- **Username rename**: pencil on the Me page — 1st free → 1,000 → 10,000
  snouts, moderation + name-taken handled inline.

## Me page

- Stat row: lifetime tickles (base + live), JOINED date replaces the season
  cell, membership chip (SLOP CLUB / FREE RANGE), "the long story" lifetime
  dashboard (fail-soft `renames_used` fetch).

## Barn + patch

- Barn visit trio: season tallies, header visits chip, tokenized colors;
  "6 7!" dialog now server-gated (fires once per account, ever).
- Country flag equips optimistically on first select — no more blank slot.
- Practice dig for the crewless; feeding notify chip (verified the
  permission prompt in a live sim run); Hungerer naming + chip clamping.
- Barn tap XP is +2 (nerf shipped in DB, client copy matches).

## Art: the camera-pass sweep

- ~99 worn-item sprites regenerated through the Codex ImageGen lane under
  the camera-only law (full 3D rendering, zero 3D rotation; near side hides
  far side) + placements re-tuned through the studio save-rel path.
- `silver_plume_helm` deprecated (rebuild-all migration already pushed);
  29 held props deliberately unswept; old `assets/images/pig.png` deleted
  (Rosie is the only pig).

## Design governance

- Sticker gains `cream2`/`bark` tokens (kills the transparent-bg ghost-text
  shadow bug); Icon gains `edit`; dingbat ruling applied (✦ · sanctioned;
  ✓ ✕ ♥ via Icon/Glyph only); impeccable critique trend 28 → 31/40.

## Verification

- Jest suite green (race parser expectations updated for `departed`).
- db-harness CHAIN green end-to-end (16 migrations + truffle prep stub).
- Sim walks: barn trio gating, rename dialog, flag first-select, season tab
  cold-open, practice dig, notify prompt.

## Numbering

- CFBundleVersion 148 via EAS remote auto-increment (143–146: 1.2.1 TestFlight lane + pod-flake attempt; 147: superseded — rebuilt to carry the 1.3 what's-new entry). DO NOT deliver build-147.ipa.. Marketing
  version 1.3 (App Store 1.2 released 2026-07-14; 1.2.1 was TestFlight-only).
