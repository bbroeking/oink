# Build 153 — 2026-07-22 (v1.3, iOS buildNumber 153)

> The deepening ship: the Lounge batch (walkable farm, seesaw, Den
> ticketing) rides with the architecture-deepening pass — ten review
> candidates + a cleanup sweep that rebuilt the app's seams without
> changing a pixel — plus the can't-tickle audit fixes (issues #3, #4,
> #5, #10, #11, #12) and server-side season claims. iOS only; the
> Android lane is parked until it gets a fresh build + Play listing
> pass (issue #32).
>
> **DB state at build:** migrations through `20260773000000` (claim
> consolidation) are PUSHED — verified live on-device 2026-07-22
> (7-tier claim-all sweep + mystery handoff on a real account).
> `20260774000000_equip_cosmetic` is drafted/harness-gated and NOT yet
> pushed; the client falls back to the RLS direct-write until the push,
> so equip works either way.

## The Lounge (from the Jul 21 batch)

- The walkable farm: tap-to-walk, realtime peers, emotes.
- The seesaw: presence seat claims, deterministic two-pig ride.
- The Den ticketing pipeline + lounge polish.

## Season pass: claims go server-side

- `claim_season_tier` / `claim_ready_tiers` route prestige-vs-normal and
  track resolution server-side; claim-all is ONE transaction with a
  server tally (tickles sum + item labels + mystery grants). Client
  keeps the legacy per-tier loop as an un-migrated-env fallback.
- Wallow prestige UI (Wall of Tiers on Account, W-rank frames) rides.

## Can't-tickle audit closed out

- **Popup collisions** (#3, #4): BuriedTruffleSheet releases its slot
  when nothing's buried (no more session-wide popup suppression);
  every unmanaged sheet/modal now holds the popup queue while open
  (14 files) so foreground polls can't stack a popup over them.
- **Offline soft-locks** (#5): home_stats boot failure walks a backoff
  schedule then shows a retry affordance; the "saddling up" gate
  retries + offers manual retry instead of hanging forever.
- **Reinstall flow** (#11): launch-popup polls re-key on session
  (silent-first-session fixed); storybook_seen mirrors server-side so
  veterans don't re-run onboarding.
- **Trough nudges** (#10): supersede live server-side (one row per
  drive); nudge cards deep-link to the Shop via the trough route.
- **Small fixes** (#12): lucky-number win toast, shop UTC-midnight
  refetch, over-cap wording ("28 banked — regen resumes under 25"),
  Wardrobe→Closet strings.
- **Push routing**: `screen: "shop"` pushes deep-link again (the route
  table + a guard test that reads the real server emit list).

## Architecture deepening (player-invisible)

- Season pass, shop catalog, dig session, config, realtime, popup
  precedence, race/duration/leaderboard/stake seams all rebuilt behind
  tested modules — CONTEXT.md's Seams section is the map. 832 tests
  (76 added), tsc clean. Also fixed en route: a fast-remount realtime
  teardown that could kill a live channel, and a dead cleanup closure.
- ADR 0005: per-surface refusal copy stays per-surface (rejected
  consolidation, recorded so reviews stop re-flagging it).

## Ship checklist

- [ ] equip_cosmetic push decision (then commit + merge the equip slice)
- [ ] `NODE_OPTIONS="--max-old-space-size=16384" eas build --local --platform ios --profile production`
- [ ] rename artifact → `build-153.ipa`, `open -a Transporter build-153.ipa`
- [ ] Apple-ID sign-in + Deliver click (user)
- [ ] post-ship: watch Sentry for claim_season_tier/claim_ready_tiers + equip errors (map fog item)
