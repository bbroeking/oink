# Build 189 — 2026-09-15 (v1.3)

## Build record

- Classification: distributable TestFlight validation build; not a release candidate.
- Release/version: 1.3. iOS build number: **189** expected — EAS increments the remote value from 188 on the first attempt; confirm in the signed IPA's Info.plist and record the actual number here if any attempt fails and consumes one.
- Android: not applicable; iOS only.
- Candidate: `main` at `ca548fd5` — build 188 (`3b761154`) plus one commit: the visit's toast line (placement B).
- Working tree at packaging: one uncommitted file from a parallel session — `components/WhileAwayModal.tsx` (the while-away recap's list height cap). **Excluded**: stashed for the packaging window and restored after, as for 188. It ships when it is committed.
- Scope: build 188 plus placement B — on the visit screen a toast sits under the header and tallies instead of over them.
- Previous TestFlight: v1.3 (188), packaged 16:20 today and opened in Transporter; delivery not confirmed at authoring. Previous public store version: v1.2.
- Release owner and rollback decision-maker: Brian Broeking.
- Target: TestFlight, local production-profile IPA, opened in Transporter after signed inspection.
- Database required: **none.** No migration since `20260915010000_satchel.sql`; `db push --dry-run --linked` confirmed up to date for 188 at 16:06 and nothing has changed since.
- Rollout: binary only. No flag flips, one-off release SQL, notifications, or public release actions are included.

## Headline

On a visit, a message never covers the score: the toast sits under the tallies now.

## User-facing

- **Visit toasts moved down a line.** "Not the blue feather", "Their pig changed its mind" and "All tickled out" now appear just under the header and tally capsules on the visit screen, so your tickle counts and visits-left stay readable while the message dwells. Everywhere else the toast line is unchanged.
- Everything in build 188: Rosie's motion pass, the ritual bubble, the friend row's art marks / straight rows / list bleed, toasts that show inside the visit and the profile sheet, and the two-line visit toast copy.

## Internal

- `ToastHost` takes an explicit `top`; default stays `insets.top + SPACE.sm`.
- The visit mounts its own host on `VISIT_TOAST_LINE` = `VISIT_CHROME_H` + `STATUS_ROW_GAP` (`components/visit/chrome.ts`; `STATUS_TAG_H` moved there so `VisitStatusRow` → `chrome` runs one way, no cycle). `VisitStatusRow.marginTop` reads `STATUS_ROW_GAP`.
- `Ceremony` and `SlideUpSheet` mount their fallback host BEFORE children — effects run in tree order and the scene's host must register last to take the calls (found on device: the ceremony's host was stealing the visit's line).
- Placement chosen from five drawn on the real screen (`Visit Toast Placement` canvas): app-wide line / under header / above strip / callout from the tile / the pig's bubble.
- Tests: `Toast.test` pins the default and passed lines and the host-stack hand-back.

## Migrations

None.

## Known issues

- Carried from 188: recruits' per-sheet pink drift; `strip_foot_specks.py` sub-alpha dust in 22 frames; the quality-contract layout budget exception, lint warning debt (0 errors) and the Expo SDK patch-update notice; `copper/idle_8.png: opaque canvas corner`.
- Friend-row ritual marks appear only after a cast/refusal in the session (`ritual_status` never reports today's targets).
- On the visit the toast's new line covers the Outside/Inside toggle for its 2.4 s dwell (by design of B; the toggle is not time-critical).

## Verification

- [x] Build classified and changelog written before EAS.
- [x] tsc clean. Full Jest at `ca548fd5`: **241 suites / 2,312 tests** green. Conformance scorecard 231/231 at zero. `expo lint`: 0 errors (513 warnings, accepted debt). `lint:web` green.
- [x] `npx supabase db push --dry-run --linked`: up to date (16:06, unchanged since).
- [x] Simulator pass (17 Pro, dev client, `ca548fd5`): visit → tap a non-matching find → toast under the tallies, header/tallies/visits-left fully visible; two-line copy.
- [ ] Signed IPA inspection: `com.broeking.ttp`, v1.3 (189), codesign, `aps-environment=production`, `get-task-allow=false`.
- [ ] Real-device checklist (owner: Brian): (1) a visit — tap a wrong find: toast under the tallies; tickle to the cap: "All tickled out" under the tallies; (2) a friend row — Bless a friend already blessed: refusal toast at the top of the Friends tab (root line), and the row's mark; (3) the ritual bubble from a fresh Bless; (4) Rosie's motion pass items from the 188 checklist; (5) Reduce Motion: toast cross-fades in place.
- [ ] Store metadata/screenshots, RC freeze/go-no-go and public-release steps: not applicable to this validation build.

## Release status

- Packaging: in progress (Claude, this session). Sentry token from `.env.sentry-build-plugin`, `NODE_OPTIONS="--max-old-space-size=16384" eas build --local --platform ios --profile production --non-interactive`; rename to `build-189.ipa`; `open -a Transporter build-189.ipa`.
- Artifact: —
- Transporter: —
- No public-store release, launch notification, or additional database activation is authorized by this build.

## Release follow-ups

- If 188 was not delivered, deliver 189 only.
