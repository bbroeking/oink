# Build 184 — 2026-09-14 (v1.3)

## Build record

- Classification: distributable TestFlight validation build; not a release candidate.
- Release/version: 1.3. Intended iOS build number: **184** (EAS increments the remote value from 183; record the actual number here after packaging).
- Android: not applicable; iOS only.
- Candidate: `main` at the freeze commit (record the SHA after the ritual commit lands).
- Working-tree scope: everything on `main` since build 183 (`cfc0867`): the weekday rituals (this build's headline), the friend-row actions panel and the 70pt list rhythm, the Snout Deep dig behind its flag (full-screen, tally sheet, loose pouch, Hungerer faces, every find pays tickles), every pig breathing (twelve-frame idles, stage breath), and the worn-avatar visit tallies.
- Previous TestFlight: v1.3 (183). Previous public store version: v1.2.
- Release owner and rollback decision-maker: Brian Broeking.
- Target: TestFlight, local production-profile IPA, opened in Transporter after signed inspection.
- Database required: **one** migration, `20260915000000_weekday_rituals.sql`, must be applied BEFORE this binary reaches a device. An older binary against the new database keeps working (old kinds stay valid, old rows keep their names); a NEW binary against the OLD database would offer the weekday ritual while the server casts the old set. Push first, build second.
- Rollout: server state first; this binary second. No flag flips, one-off release SQL, notifications, or public release actions are included.

## Headline

Every day has one blessing and one curse, and you wear them: seven of each, locked to the weekday, cosmetic only. It's Bacon Friday.

## User-facing

- Blessings and curses are a week now. Monday is Cloud Nine and Pickle Brine, Tuesday Bubble Bath and Topsy-Turvy, Wednesday Butterfly Crown and Pipsqueak, Thursday Confetti Snout and Little Raincloud, Friday Golden Hour and Bacon Bits, Saturday Firefly Night and Hiccups, Sunday Sunday Best and Old-Timey Pig. The day flips at midnight UTC, the same clock as the daily cap.
- Rituals no longer touch play. No regen change, no lucky boost, no free tickles or snouts, no snout pinch, no tap slips. Each is something you see on your pig or your Barn for six hours.
- The Barn shows the wash (amber, briny green, dusk with fireflies, sepia with grain); the pig shows the rest (floating on a cloud, bubbles, upside down, half size, a raincloud, bacon stripes, hiccups, a bow tie, a butterfly, a monocle); tickles pop confetti under Confetti Snout.
- Friends see it. Visiting a Barn shows the host's wash and pig exactly as the host sees them, tickling a Confetti Snout host pops confetti in their Barn, and a friend's profile portrait wears the still parts (skin, size, flip, forced cosmetics).
- The Field Guide's Mud Wrap page becomes Rituals.
- Effects already on you from the old set keep their old names and icons until they expire (twelve hours at most).
- Friends list: one "…" per row opens an anchored actions panel (Visit · Bless · Curse · Pin · Profile); rows sit at 70pt with even gaps.
- Every pig breathes: twelve-frame idles and a stage breath, and the barn-visit tallies show both pigs as dressed.

## Internal

- `constants/ritualFx.ts` is the recipe contract (kinds, channels, `mergeRitualFx`, `fxWash`, `staticPigFx`); `hooks/useRitualPresentation.ts` folds active effects into one presentation; `BarnOverlay` takes `scene`, `PigStage` takes `ritual` (transforms on the breath wrapper, so raster and Rive agree), `PigAvatar` takes static channels only; `ConfettiBurst`, `PigRitualFx` (followers, bubbles, hic bubble), `CosmeticGlow` extracted from `AnimatedCosmetic`.
- `utils/rituals.ts`: seven-long Monday-first rotations on `isoWeekdayUTC`; `LEGACY_RITUAL_META` for retired kinds; no season-flag branch. `useRitualCaster` prefers the server's `bless_kind` / `curse_kind` from `ritual_status`.
- Removed from the Barn: the lucky boost, its consume-on-lucky clear, and the tap-miss curse; `luckyPig.ts` loses the boost input.
- Reduce Motion: transforms freeze, particles render one frame, Hiccups keeps a persistent "hic!" bubble so it stays identifiable.
- Visitor path: `active_effects_of` → `fetchActiveEffectsOf` → `useVisitorEffects` → `useRitualPresentationFor`; `BarnVisitModal` and `UserSheet` read it; `PrestigeAvatar` switches to the worn portrait while a static ritual is on.
- `utils/sound.ts` gains a playback `rate` option (unused until an oink cue exists; Pipsqueak's pitch is deferred).
- Charter: `SKILL.md` decision log entry 2026-09-14; plan `docs/design/2026-09-14-weekday-rituals-plan.md`.

## Migrations

Apply before this build:

- `20260915000000_weekday_rituals.sql` — widens both kind CHECKs (old kinds kept); `daily_blessing_kind` / `daily_curse_kind` on `ISODOW`, no `app_config` read; `send_blessing` and `send_curse` flat 6h × alignment factor, no payouts, no pinch; `ritual_status` adds `bless_kind` / `curse_kind`; `ritual_push_notify` copy per kind (carried from `20260705100000`, the latest def); new `active_effects_of(p_target)` (friend, crewmate or self; refuses blocked pairs; no sender); `unlock_field_guide_page` admits `rituals` and existing `mud_wrap` unlocks are carried over. Every SECURITY DEFINER function revoked from PUBLIC. Validated on the throwaway Postgres harness: 11 checks green.

## Known issues

- Ritual icons and the four forced cosmetics are first-pass generated art. Their RelSpecs are hand-tuned in `constants/ritualFx.ts` (`RITUAL_ITEM_REL`) against the rest pose, because the placement studio only enumerates shop items; if one sits wrong on device, tune the values there or teach the studio about the table.
- Bacon Bits is a rose tint on the whole pig plus a rasher band across the belly, not a full stripe skin; a true striped skin sprite is a follow-up.
- Pipsqueak's higher-pitched oink is not wired (no oink cue asset yet).
- Two pigs with Hiccups hop in sync (wall-clock interval).
- Pre-existing quality-contract layout budgets on `components/Friends.tsx` (shrink-to-fit text, line-clamp count) are over; unrelated to this build's scope and accepted as a non-RC exception.
- Lint warning debt (warning-only) and the Expo SDK patch-update notice remain accepted non-RC exceptions, as in 183.

## Verification

- [x] Build classified and changelog written before EAS.
- [ ] Authorized migration pushed and verified read-only on the linked project (`daily_blessing_kind()` returns the weekday's kind; `ritual_status()` carries `bless_kind`).
- [x] tsc clean; full Jest **236 suites / 2,232 tests** green; `test:quality` security contract green (the one failing contract is the accepted Friends layout budget); design-system scorecard `0 · 224/224`.
- [ ] Signed IPA inspection: bundle id, version/build, codesign, entitlements.
- [ ] Real-device checklist (owner: Brian): (1) Barn on the day's blessing + curse together — wash and pig both present, tickles still count; (2) friend visit — host's ritual visible; (3) Reduce Motion on — every ritual still identifiable; (4) Inbox — an old effect still names itself; (5) cast at 23:59 UTC vs 00:01 — toast names the server's kind.
- [ ] Store metadata/screenshots, RC freeze/go-no-go and public-release steps: not applicable to this validation build.

## Release status

- Packaging: pending.
- Artifact: pending.
- Transporter: pending; Brian owns Apple ID sign-in and the final Deliver click.
- No public-store release, launch notification, or additional database activation is authorized by this build.
