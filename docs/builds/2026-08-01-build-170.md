# Build 170 — 2026-08-01 (v1.3, iOS buildNumber 170)

## Build record

- Classification: distributable build (not a release candidate)
- Release/version: 1.3
- iOS build number: 170; EAS remote version incremented from 169 during the
  successful local build and was confirmed at 170 afterward
- Android version code: not applicable; iOS-only build
- Candidate code commit: `6b77a1452b4000a7b516329ffbe451af8ae8eb64`
- Working-tree state: candidate commit is clean before this required changelog;
  the only intended pre-build diff is this untracked build record (preflight
  SHA-256 `7647709fcc22fedba354fb94c886efd731319f7a9061333d4c6317f4e8187f73`)
- Previous verified artifact: build 169; build 170 supersedes it with clean,
  published `main` provenance after PR #52 and PR #53 were merged
- Previous live store build: pending confirmation from Brian
- Release owner / rollback decision-maker: Brian Broeking
- Target: TestFlight via local production IPA and Transporter
- Planned release date: pending exact-binary device verification
- Database state required: production applied through
  `20260801010000_sounder_invite_security_grants.sql`; no database push is
  authorized or required for this rebuild
- Feature flags/timed events: existing flags unchanged; the server-owned
  commuter clock remains the active implementation
- Deferred work: App Store submission/release and any release-only activation
- RC status: not applicable
- Build result: successful at 2026-08-01 11:56 EDT; no failed EAS attempts and
  only build number 170 was consumed for this invocation

## Headline

The current game, commuter-safe Truffle Patch clock, and expanded Sounder
recruiting are rebuilt from a clean, fully published `main` commit.

## User-facing

- Sounder leaders can browse all-time truffle diggers or search by username,
  invite players beyond their friends, and offer consent-based Sounder switches.
- The Truffle Patch uses the server-owned commuter schedule, protecting window
  eligibility from device-clock and timezone manipulation.
- Includes the full player-facing scope already captured by builds 168 and 169,
  now with all source, art, migrations, and tooling published to GitHub `main`.

## Internal

- PR #52 and PR #53 are present in `main` history. During integration, PR #52's
  older phone-local proposal was resolved in favor of the newer, tested
  server-owned Eastern clock and migration `20260799000000`.
- The complete local workspace was checkpointed and pushed before packaging;
  the merge tree differs from that checkpoint only by PR #53's security-grant
  documentation.
- GitHub accepted four generated PDFs above its recommended 50 MB threshold;
  every individual file remains below the 100 MB hard limit.

## Migrations

- No new migration relative to build 169.
- `20260799000000_server_clock_commuter_windows.sql` is the active commuter
  clock implementation.
- `20260801000000_sounder_invite_any_player.sql` adds leader-wide recruiting,
  consent-based switching, cooldown state, and candidate discovery.
- `20260801010000_sounder_invite_security_grants.sql` revokes default and
  anonymous execution from the new security-definer functions.
- PR #52's superseded `20260740000000_dig_schedule_commuter_local.sql` is not in
  the final merge tree and must not be applied.

## Known issues

- Complete real-device core-loop/recruiting smoke, notification paths,
  smallest/largest layouts, accessibility, offline/auth recovery,
  purchase/restore, links, camera paths, and Sentry inspection remain pending
  with Brian.
- Production permits Sentry symbol upload failure and excludes symbols, so
  crash symbolication may be reduced.
- App Store reviewer-account and previous live-build confirmation are pending.

## Verification

- [x] Classification: distributable TestFlight build, not an RC
- [x] Source published: GitHub `main` and local `main` both point to `6b77a14`
- [x] Pre-merge integration: TypeScript passed; 40 suites / 501 tests passed;
  changed PR files had zero lint errors
- [x] Post-merge `npm run quality:check`: passed — 324 sprites, layout and
  security contracts, TypeScript, 8 layout suites / 78 tests, and 11 security
  suites / 202 tests
- [x] Post-merge full Jest: 118 suites / 1,259 tests / 0 snapshots
- [x] Post-merge standalone TypeScript: passed
- [x] `npm run quality:check:full`: passed — quality/layout/security contracts,
  324 sprites, TypeScript, 118 suites / 1,259 tests, production-source lint,
  simulator-free iOS export, database harness, and linked database lint
- [ ] Repository-wide `npx expo lint`: accepted pre-existing exception — 67
  errors / 150 warnings are dominated by imported three.js/vendor sources,
  local temporary Python packages, test/tool globals, and the Deno edge
  function. The release-grade production-source lint passed. Risk: repo-wide
  lint coverage remains noisy outside bundled production TypeScript.
- [x] Supabase dry run: remote database is up to date; zero queued migrations,
  seeds, or roles; no push performed
- [x] App configuration: v1.3, `com.broeking.ttp`, light UI, production push,
  Sign in with Apple, `applinks:ticklethepig.com`, and required privacy strings
- [x] Production environment: privacy URL, Sentry DSN, IAP enabled, real
  RevenueCat iOS public key, `GYM_INCLUDE_SYMBOLS=false`, and
  `SENTRY_ALLOW_FAILURE=true`
- [x] Feature flags reviewed: Sounder, purchases, and spotlight remain enabled;
  runtime `coop_dig` remains server-controlled
- [x] Release notes: no new in-app entry required because build 170 is a
  provenance rebuild of build 169 with no new player-facing delta
- [ ] Previous live-store build and reviewer test-account confirmation (Brian)
- [ ] Complete real-device pre-ship checklist
- [ ] Exact TestFlight-installed binary verification
- [x] Local production archive/export: succeeded — Expo Doctor 17/17,
  CocoaPods and Metro completed, Xcode reported `Archive Succeeded`, and EAS
  exported the signed production IPA
- [x] Artifact renamed to `build-170.ipa`; ZIP integrity reported no compressed
  data errors
- [x] Signed IPA metadata: `com.broeking.ttp`, version 1.3, build 170, minimum
  iOS 15.1, arm64, no app extensions, and camera/microphone usage descriptions
- [x] Deep/strict code-sign verification passed; App Store distribution
  identity/team `Z8F75879GQ`; production push, Sign in with Apple, and
  `applinks:ticklethepig.com` entitlements are present; `get-task-allow=false`
- [x] Embedded App Store provisioning profile matches the application ID,
  production push environment, and expires 2027-03-20

## TestFlight “What to Test”

As a Sounder leader, open **Recruit any snout**. Confirm all-time truffle order,
username search, free-player invites, decline/cooldown, consent-based switching,
and block behavior. Verify Truffle Patch opening/countdown behavior around the
server-owned commuter windows. Repeat the Barn, shop/IAP, notification, link,
camera, offline/reconnect, sign-out/sign-in, and accessibility core smoke.

## Release status

- Artifact: `/Users/bbroeking/projects/oink/build-170.ipa` (91,877,672 bytes;
  SHA-256
  `e3e319b6eac54c509c908457778e4e79515a6c7f5d68dd0fbbf16a3407318535`)
- Signed-binary inspection: passed locally; 1,116 archive entries / 121,980 KiB
  uncompressed app payload, no extensions
- Transporter: pending; Apple-ID sign-in and final Deliver belong to Brian
- App Store Connect processing: pending
- Database: applied through `20260801010000`; no push authorized or required
- TestFlight device pass: pending
- Store release: not requested
