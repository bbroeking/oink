# Build 149 — 2026-07-14 (v1.3, iOS buildNumber 149)

> Supersedes build 148 (never delivered — its what's-new was stale and it
> lacked the same-day founder batch below). This is the 1.3 review binary:
> everything in 148, plus the evening's funnel + layout decisions, PR #27,
> and a what's-new that matches what actually ships.

## Since build 148

### The banner calls the dig (founder layout pass)

- **HungerHero**: the dig CTA lives INSIDE the banner sticker now — header
  row (thumb + loop line) is its own Pressable; the footer is a real primary
  "dig this feeding ›" button when the patch is open, and a quiet muted
  status line ("dug this feeding — opens in …" / "he's guarding — opens
  in …") when it isn't. No more detached full-width ghost pill.
- **One countdown per region**: the Sounder-card locked pill drops its
  countdown ("dug this feeding ★" / "he's guarding") — the WindowStrip
  caption directly above owns the schedule, and it now reads from the shared
  `FeedingCta` clock so the two can never disagree by a minute again.

### The dig receipt (founder layout pass)

- **TrufflePatch end state** rebuilt as a receipt: centered verdict headline,
  left-aligned ledger rows with an icon column (golden-truffle row with
  first-mint gift line folded in, joy/carry/echo rows), Burrow Book discovery
  as a tappable row. Whisper + chips retire at end; the zero-value motes chip
  never renders. Truffle/relic reveals sit proud of their tile (1.12×).

### Ready claims surface at the top

- **ClaimAllBar** fires at ≥1 ready (was ≥2): singular copy names the reward
  ("25 tickles is ready — claim ›"); the ≥2 sweep copy unchanged.
- **SeasonEndBeat** never shows the ready state — it teases the next
  UNREADY reward ("80 XP more and …") or the season promise; renders
  nothing when every leftover is ready.

### Leaving a Sounder → a dismissible call back

- Leaving stamps `sounder_left:{uid}`; the path machine regresses a
  crewless leaver to the join step with leaver copy ("HERDLESS AGAIN — the
  patch digs better with a herd") and a quiet ✕; dismissal retires the card
  (join door stays as the ambient path). Re-joining clears the dismissal so
  a later leave re-arms the nudge. New `__tests__/sounderPath.test.ts`.

### Spotlight ships LIVE

- `SPOTLIGHT_ENABLED = true` (was `__DEV__`): the dim-screen coach-mark
  fires once per install on the join step, hole cut around **just the join
  door** (founder call), caption "your herd digs deeper — slip into a
  Sounder ›" + "maybe later ›". Verified live in sim: hole placement,
  touch fall-through, dismissal, leaver interplay.

### Renaming belongs to pigs, not herds

- **Username rename** entry moved to a "Change your name" settings row
  (pencil, first row above "What's new"); identity-card pencil removed.
- **Sounder renaming removed** from the client (SeasonGuideModal affordance
  + `renameCrew` wrapper stripped; server RPC stays dormant); founding
  blurb no longer promises a rename.

### Everything else

- **PR #27**: leaderboard tiebreak (merged after 148 was built).
- **Release notes v1.10.0** rewritten to match the shipped 1.3: adds "The
  banner calls the dig" + "A dig worth reading", rename item points at the
  settings row.
- Flag optimistic mount + `bumpHomeStats` cross-tree nudge (popup-path
  allegiance choice mounts the flag instantly).

## Verification

- `npx tsc --noEmit` clean; Jest 41 suites / 513 tests green (12 new).
- Live sim walk (FunnelWalker): banner footer states, shared clock
  agreement, claim bar singular, end-beat tease, leave → spotlight →
  leaver card → dismiss → join door, rename settings row, receipt card.

## Numbering

- CFBundleVersion 149 via EAS remote auto-increment. Marketing version 1.3.
  **Deliver build-149.ipa only** — 147/148 are superseded and deleted.
