# Build 104 — 2026-07-06 (iOS buildNumber 123)

Season-1 readiness build: the Sounder tab, the scuffle overhaul, Prize
Ribbons, the season renumber, Season-1 achievements, and the beta-founder
reward art. All Season-1 surfaces remain flag-dark until the Jul-12 flip.

## Sounder tab (Friends hub)

- Full invite-matchmaking design: crew-state header ("Find your Sounder" /
  "Your Sounder"), sun-pill segment bar, single muster sticker (invites →
  browse → found form), non-actionable invite strip, pass-the-crown sheet.
- Roster portraits now show equipped looks (crew_state lacks avatar fields;
  client profiles read). Sheet scrim flash fixed (transparent modal, ink
  veil fades, panel slides).

## Mud Scuffle overhaul

- War strip + SounderCard CTA: opponent, plain-words rope, day, countdown,
  inline Dig/Hold/bog deep links. War page hierarchy: WHO / WHERE THE ROPE
  IS / WHAT TO DO NOW; fronts de-jargoned below the fold + explainer.
- Joy-reclaim fiction everywhere (scoreboard, drain line, resolve copy) +
  ReclaimSlam bursts on every minigame scoring beat; Hungerer flinches.
- SnoutHook rebuilt to sibling parity (hook rig, vignette, find art);
  submit payloads hardened via claimableFinds() across all three dig games
  (fixes the reel-in "malformed array literal" crash).
- Dev war-state sheet (dev builds only) to preview any war state.

## League + renumber + achievements (dark)

- Sounder League table ranked by Prize Ribbons ("new banner" while
  provisional); Leaderboard scope + season placard updated.
- Seasons renumbered: greedy/generous era = Season 0, The Great Hunger =
  Season 1 (client + docs; server renamed in 20260709).
- Scuffle achievements chip (8 new achievements, 3 titles — server 20260710).

## Season-0 finale readiness

- ART GATE CLEARED: beta_founder_ribbon art bundled (HAT_IMAGES +
  conservative neck RelSpec; placement-studio pass before flip).
- Founder's-gift reveal: box wobble→burst, tier-generic reward chips,
  snout count-up; dev tier-cycler on the season tab.
- Great Hunger tale = real narrated cinematic (expo-video in 103; the
  4.1 MB bundled cut, marketing CTA trimmed).

## Ops

- Migrations 20260708/09/10 applied to prod (ribbons, renumber,
  achievements). DB harness suite committed: scripts/db-harness/run.sh.
- Client tests 442 across 38 suites; expo-doctor 17/17 (packages bumped to
  SDK-52 expected; obsolete expo-dev-menu patch removed).
- Device checks this build: SnoutHook + slams, tale audio vs silent
  switch, founder box, Sounder tab states, league table (mocks are
  dev-only — TestFlight shows the sparse real board).
