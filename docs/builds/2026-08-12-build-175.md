# Build 175 — 2026-08-12 (v1.3, iOS buildNumber 175)

## Build record

- Classification: distributable build (not a release candidate)
- Release/version: 1.3
- iOS build number: 175. EAS remote version was 174 before invocation; the
  production profile auto-incremented it to 175 as planned.
- Android version code: not applicable; iOS-only build
- Candidate commit: `6a30b3dcb98e46610d3299d4b89ea6d4ce22d0ff` plus an
  intentional dirty workspace
- Working-tree state: 122 tracked files changed (5,620 insertions / 6,508
  deletions) before this changelog. The tracked binary-diff SHA-256 was
  `9d803523144bf853715e6b333f4fa6cb44ccf8584f928962d488bbd0cbcf1ddd`;
  31 untracked files had a sorted content-manifest SHA-256 of
  `9c362a00f6aad10c107b42bc4c4b1a67f4d3a77a0f2e4f50cefa142a3eae4a41`.
  The dirty scope is primarily the Expo 52→57 native/runtime upgrade,
  Homegrown Adventure research/prototypes, the two already-applied 20260812
  migrations, and the Sounder-cap change. This is accepted for a TestFlight
  test artifact only; it is not an RC provenance standard.
- Previous inspected artifact: build 172 (rejected for the sample AdMob app
  id). Build 171 remains the previous usable local artifact. No build-174 IPA
  is present locally even though the EAS remote sequence is 174.
- Previous live App Store build: v1.2, released 2026-07-14
- Release owner / rollback decision-maker: Brian Broeking
- Target: TestFlight via local production IPA and Transporter
- Planned release date: pending TestFlight verification
- Database state: after the build, Brian explicitly authorized all three queued
  migrations. `20260803000000`, `20260803010000`, and `20260812020000` were
  applied successfully to production on 2026-08-12. The Sounder backend now
  supports eight seats; both rewarded-ad enablement switches remain off.
- Feature flags/timed events: rewarded ads remain dark/unconfigured in
  production; Great Hunger is extended through 2026-09-12 by the already-live
  `20260812000000` migration.
- Deferred work: physical-device SDK 57 acceptance, TestFlight exact-binary
  smoke, Android SDK/JDK compile and Android AdMob production ID, rewarded-ad
  activation/deployment, store submission
- RC status: not applicable

## Headline

Sounders can now grow to eight pigs, packaged on the upgraded Expo 57 native
runtime for TestFlight acceptance.

## User-facing

- Sounders now hold up to eight pigs instead of four. Invite, knock, accept,
  discovery, reserved-seat copy, and the database insert backstop share the
  same limit.
- “What’s new” announces the larger Sounder.

## Internal

- Upgraded Expo SDK 52→57, React Native 0.76→0.86, React 18→19, Reanimated 4,
  Supabase JS, CocoaPods/native iOS scaffolding, Android Gradle scaffolding,
  and related test/config compatibility. Full notes:
  `docs/upgrades/2026-08-12-expo-57.md`.
- Fixed the production AdMob config guard so an iOS build validates the iOS
  app ID without incorrectly demanding an Android production ID; Android
  production builds still fail closed without their own ID.
- Added lint-rule annotations for two React test probes whose mutable handles
  are intentional test harness state.
- Adventure prototypes, landing-page work, and research are included in the
  dirty workspace but do not activate a new mobile gameplay lane.

## Migrations

- `20260812000000_extend_great_hunger_season.sql` — already applied in
  production; extends Season 1 through 2026-09-12.
- `20260812010000_auto_apply_season_pass_tickles.sql` — already applied in
  production; applies claimed pass tickles directly to season/lifetime totals.
- `20260812020000_sounder_capacity_eight.sql` — applied after explicit user go;
  raises all current invite/knock/accept/discovery gates and the
  concurrency-safe insert trigger from four to eight.
- `20260803000000_rewarded_ads.sql` and
  `20260803010000_rewarded_ads_reporting.sql` — applied alongside the Sounder
  migration after explicit user authorization. They remain feature-dark:
  `app_config.rewarded_ads=false` and
  `rewarded_ad_settings.ad_refill.enabled=false`.

## Known issues

- The Expo 57 upgrade has 504 non-blocking lint warnings (React
  compiler-readiness, import ordering, and existing TypeScript style). Lint
  exits successfully with zero errors.
- The first full quality attempt failed only because Colima/Docker was stopped;
  after starting it, the full database harness passed.
- The final `npx supabase db lint --linked --level error` step hung in the npm
  wrapper after every prior full-quality gate passed. The identical installed
  CLI command completed successfully with zero findings. `npm run
  quality:check` also reports QUALITY PASS.
- SDK 57 still needs physical-iPhone acceptance for sign-in, deep links, push,
  camera, audio/video, Rive/Skia, purchases, rewarded-ad darkness, and the
  core Sounder flow. This artifact is for that TestFlight testing, not store
  release.
- Signed-IPA inspection found no `PlugIns/TTPStickers.appex`. Builds 171 and
  172 also omit the extension, and the Xcode project's existing "Embed App
  Extensions" phase has no files, so this is a pre-existing packaging defect
  rather than a build-175 regression. Sticker-pack testing is blocked until a
  separately scoped native-project fix and replacement build.
- The RN Google Mobile Ads build script warned that `ios_app_id` was absent
  from `app.json`, but the exported signed IPA contains the intended production
  `GADApplicationIdentifier` (`ca-app-pub-1425739013457484~8765193677`). Treat
  the script message as a false-positive warning for this artifact.
- The working tree is intentionally broad and dirty. Commit/push scope must be
  reviewed separately; producing this IPA does not authorize committing every
  pre-existing change.

## Verification

- [x] Classification: distributable TestFlight build, not an RC
- [x] App config: v1.3, bundle ID `com.broeking.ttp`, production push, Sign in
  with Apple, `applinks:ticklethepig.com`, production AdMob iOS ID guard
- [x] Production environment: privacy URL, Sentry DSN, IAP, RevenueCat,
  production AdMob iOS app/rewarded-unit IDs; symbols excluded and Sentry
  upload allowed to fail by existing policy
- [x] TypeScript: clean
- [x] Full Jest suite: 125 suites / 1,283 tests passed
- [x] Focused Sounder tests: 2 suites / 22 tests passed
- [x] Lint: zero errors / 504 warnings
- [x] Quality/security/layout contracts and 324-sprite integrity: passed
- [x] Simulator-free iOS Hermes export: passed (3,812 modules, 837 assets)
- [x] Database harness: passed all NOTICE checks, including invite seats,
  join-request races, and current migrations
- [x] Linked database lint: zero error-level findings via installed Supabase CLI
- [x] Remote migration boundary: 20260812000000 and 20260812010000 applied;
  initial planning identified 20260803000000, 20260803010000, and
  20260812020000 as the only pending files
- [x] Production database push: dry-run listed exactly those three migrations;
  Brian explicitly authorized all three, and `supabase db push --linked
  --include-all` applied them successfully on 2026-08-12
- [x] Post-push database verification: all three local/remote versions match;
  linked error-level lint is clean; a remote schema dump shows the membership
  trigger and six seat gates use 8 and discovery uses `BETWEEN 1 AND 7`; the
  two production rewarded-ad switches were queried and remain false
- [x] Production config guard probes: valid iOS config succeeds; sample iOS ID
  fails closed; sample Android ID fails closed for Android
- [x] Local production archive/export: succeeded with EAS-managed App Store
  distribution certificate/profile; Expo Doctor passed 19/19 during build
- [x] Signed IPA inspection: archive integrity and deep/strict code signature
  valid; bundle `com.broeking.ttp`, version 1.3 (175), production push, Sign in
  with Apple, `applinks:ticklethepig.com`, camera/microphone descriptions,
  production AdMob ID, `get-task-allow=false`; provisioning profile expires
  2027-03-20. Expected sticker extension is absent as documented above.
- [x] Actual EAS build number 175 recorded; artifact renamed to
  `build-175.ipa` (99,346,890 bytes; SHA-256
  `e85b8b45126c9e65b4445b869d516472cfa9fd359d1190c6799fe0d80264bf5a`)
- [ ] Real-device pre-ship checklist (pending with Brian)
- [ ] Exact TestFlight-installed binary verification (pending)

## TestFlight “What to Test”

Create or use a Sounder with more than four pigs. Verify the roster count and
all eight seat pips, send enough invites to reserve the open seats, cancel one,
accept invites through the eighth member, reject a ninth with friendly “full”
copy, and confirm full Sounders disappear from discovery. Then run the SDK 57
core smoke: cold launch, sign-in, Barn tickles, shop/purchase restore, visits,
Sounder/Season/dig, camera QR, notification routes, audio/video, Rive/Skia,
background/resume, offline/reconnect, and the sticker extension. Confirm no
rewarded-ad UI appears while the production flag/backend remain dark.

## Release follow-ups

- `sounder-capacity-eight`
- `expo-57-native-upgrade`

## Release status

- Artifact: `/Users/bbroeking/projects/oink/build-175.ipa`; locally exported,
  signed, and inspected. Not an RC; exact-device testing remains pending.
- Transporter: pending; Apple-ID sign-in and final Deliver belong to Brian
- App Store Connect processing: pending
- Database: ready. Sounder-cap and both dark rewarded-ad migrations are live;
  rewarded ads remain disabled at both production switches.
- TestFlight device pass: pending
- Store release: not requested
