# Build 86 — 2026-06-06

## Headline

Build 85 + **pre-blocking the Visit button** when a visit can't succeed.
Supersedes 82–85 — deliver this one.

## What changed since build 85

- **The "Visit their Barn" button is now disabled up-front** when a visit
  wouldn't work, instead of opening the visit screen just to show a dead-end
  pop-up. `UserSheet` checks `barn_visit_status` on open and the button reads:
  - **"Come back in 2h 15m"** — you're locked to a different barn (one friend / 3h),
  - **"Out of tickles to visit"** — your bank is empty,
  - **"Their pig is resting — come back soon"** — that pig hit its hourly tap ceiling.
  The visit modal keeps its own guards as a backstop for the moment before the
  status RPC resolves.

## Internal

- `components/UserSheet.tsx`: `visitGate` (from `barn_visit_status`) → derived
  `visitBlock` → disabled `visitBtn` variant. Client-only, no migration.

## Build notes

`NODE_OPTIONS="--max-old-space-size=16384"`, `eas build --local --platform ios
--profile production --non-interactive` (COCOAPODS_DISABLE_STATS=true to dodge the
null-byte flake). Rename to `build-86.ipa`, upload via Transporter.
