# Build 178 — 2026-08-27 (v1.3, iOS buildNumber 178)

## Build record

- Classification: distributable build for TestFlight; not a release candidate
- Release/version: 1.3
- iOS build number: 178; confirmed in the exported app and in the post-build
  EAS remote version state
- Android version code: not applicable; iOS-only build
- Candidate commit: `dd4516ba4ff8495f9a7c6c8bdca90390c70c141f` plus an
  intentional dirty workspace
- Working-tree state before this changelog: 211 tracked status entries and 100
  untracked status entries; tracked diff is 211 files with 10,463 insertions and
  10,390 deletions. The tracked binary diff SHA-256 is
  `f1c516ee7a2edf75a92b7d76631fef06e7e24ddb1b4996e08ec9b6d53c524470`;
  the sorted untracked content-manifest SHA-256 is
  `de09a609068a10e0ab6e62826cc4514c947fdf8cba3d355b2a35dbe59782414a`.
  This broad dirty scope is accepted only for a non-RC TestFlight artifact and
  is not acceptable provenance for a store candidate.
- Branch state: local `main` is 6 commits ahead and 38 commits behind
  `origin/main`. No source push is implied by building or uploading this IPA.
- Previous inspected artifact: build 177, produced locally on 2026-08-26
- Previous live App Store build: v1.2, released 2026-07-14
- Release owner / rollback decision-maker: Brian Broeking
- Target: TestFlight via local production IPA and Transporter
- Planned release date: pending TestFlight verification
- Build changelog: dark-launch the unfinished Mote Machine and its supporting
  player-facing Mote affordances behind one compile-time visibility flag
- Database state required: production remains applied through migration
  `20260826040000`; no migration or database push is required for this build
- Feature flags/timed events: `MOTE_MACHINE_VISIBLE=false`; existing
  server-controlled flags and Feeding scheduling remain unchanged. Mote wallet
  crediting and replay-safe receipts remain live so balances can accrue for a
  later relaunch.
- Deferred work: Mote Machine lore, randomizer design, Rive rebuild, and
  player-facing relaunch; real-device smoke; exact TestFlight-binary acceptance;
  App Store Connect processing; public-store release
- Rollback: stop rollout for an auth, economy, notification, purchase, or
  data-integrity regression. Re-enabling the Mote Machine requires a new build;
  database repair must be a reviewed forward migration.
- RC status: not applicable

## Headline

The unfinished Mote Machine is dark-launched while its story, randomizer, and
Rive experience are redesigned.

## User-facing

- Hide the Mote Machine card, post-dig machine CTA, Mote digging-stat rows, and
  Mote-specific profile copy.
- Redirect direct attempts to open the disabled machine back to the Season tab.
- Keep Shimmer Pockets visible as Dig Finds without exposing an unusable Mote
  balance or machine promise.

## Internal

- Added one `MOTE_MACHINE_VISIBLE` compile-time flag, defaulting to `false`, and
  applied it at parent entry points plus defensive component and route guards.
- Keep the existing Mote wallet, Shimmer credit trigger, idempotent spin RPC,
  stored balances, Rive source, and tests intact for continued redesign.
- Added source-contract coverage that pins the shipping flag off and verifies
  every player-facing Mote Machine surface is gated.

## Migrations

- None. The linked production database is already current through
  `20260826040000`; `npx supabase db push --dry-run` returned `upToDate: true`
  with zero migrations, seeds, or roles pending. No database push was run.

## Known issues

- The workspace is intentionally broad and dirty. Producing this test artifact
  does not authorize committing or pushing all current source changes.
- Motes continue accruing invisibly from submitted Shimmer Pockets while the UI
  is dark. This preserves player value but requires the later relaunch to
  explain pre-existing balances clearly.
- The Mote Machine implementation and Rive asset remain bundled even though all
  production entry points are hidden; this change is a visibility dark launch,
  not bundle-size removal.
- Real-device pre-ship smoke, changed-screen accessibility/size checks, and
  exact TestFlight-installed acceptance remain manual gates owned by Brian.
- Jest passes but emits its existing open-handle shutdown warning.
- Lint reports 387 warnings and 0 errors; the warning-only debt is unchanged
  from build 177 and accepted for this non-RC TestFlight artifact.
- Expo Doctor passes 19 of 20 checks and reports 18 available Expo SDK 57 patch
  updates. The locked dependency set passed TypeScript, all tests, export, the
  database harness, and the linked database lint; patching is deferred to avoid
  broadening this build.
- Production intentionally permits Sentry symbol-upload failure and excludes
  symbols, so crash symbolication may be reduced.
- The Google Mobile Ads Xcode phase emitted an `ios_app_id key not found`
  warning, but direct inspection of the exported app confirms
  `GADApplicationIdentifier=ca-app-pub-1425739013457484~8765193677`; the
  packaged configuration is present.

## Verification

- [x] Classification: distributable TestFlight build, not an RC
- [x] Candidate commit, dirty-tree scope, and source fingerprints recorded
- [x] Production flag inspected: `MOTE_MACHINE_VISIBLE=false`
- [x] App config inspected: Tickle the Pig v1.3, portrait,
  `com.broeking.ttp`, production push, Sign in with Apple, associated domain
  `applinks:ticklethepig.com`, microphone privacy description, icons, splash,
  production privacy URL, Sentry, IAP, RevenueCat, and AdMob configuration
- [x] EAS auto-incremented the iOS build number from 177 to 178; post-build
  remote version state is 178
- [x] Remote database dry-run is up to date through `20260826040000`; no push
  required or performed
- [x] Focused Mote Machine verification: 5 suites and 22 tests passed
- [x] TypeScript passed
- [x] Full Jest suite: 149 suites and 1,376 tests passed; existing open-handle
  warning recorded
- [x] Lint: 0 errors and 387 warnings; 23 warnings are auto-fixable
- [x] Fast quality loop passed contract checks, 324-sprite integrity,
  TypeScript, 78 layout tests, and 203 security tests
- [x] Full quality gate passed all 10 stages, including the complete Jest
  suite, production-source lint, simulator-free iOS Metro export, configured
  database harness, and linked database lint with zero findings
- [x] Expo Doctor: 19/20 checks passed; dependency patch drift accepted above
- [ ] Reviewer account validation (pending with Brian)
- [ ] Real-device pre-ship smoke, accessibility, and smallest/largest supported
  layout checks (pending with Brian; accepted before non-RC TestFlight packaging)
- [ ] Verify on device that Season has no Mote card, Shimmer receipts have no
  machine CTA, digging stories omit Mote rows, and a direct machine route returns
  to Season (pending with Brian)
- [x] Local production archive/export succeeded with EAS CLI; archive and IPA
  export both completed successfully
- [x] Artifact renamed to `build-178.ipa`; 99,415,360 bytes; SHA-256
  `ce68d91508bb404248a8c561edbccfc39478a28bc4147235ab838cd39a6b9d48`
- [x] IPA ZIP integrity passed with no errors
- [x] Exported app identity is `com.broeking.ttp`, version 1.3, build 178,
  minimum iOS 16.4, arm64
- [x] Deep strict code-signature verification passed; signed by
  `iPhone Distribution: Brian Broeking (Z8F75879GQ)`
- [x] App Store profile `367a5356-ea45-4062-8cc0-7b13bc11f0c6` is active
  through 2027-03-20; `get-task-allow=false`, `beta-reports-active=true`
- [x] Production push, Sign in with Apple, associated-domain, application, and
  team entitlements are present; no app extensions are bundled
- [x] Packaged AdMob application ID is correct and 19 privacy manifests are
  present
- [x] Post-build database dry-run remained up to date with zero migrations,
  seeds, or roles pending; no database push was run
- [ ] Exact TestFlight-installed binary acceptance (pending with Brian)

## TestFlight “What to Test”

Confirm the Season screen has no Mote Machine card. Complete a Truffle Patch
that contains a Shimmer Pocket and confirm the receipt says only that the pocket
was found, with no Mote tally or machine CTA. Open your own and another pig's
digging story and confirm Mote rows/copy are absent. Attempt a saved direct link
to `/mote-machine` and confirm it safely returns to Season. Then run the normal
v1.3 core smoke for Barn tickling, Sounders, Dig, Hat/Bow equipment, Feeding
alerts, preset Oinks, auth, purchases, notification routing, and reduced motion.

## Release follow-ups

- No new follow-up is attached specifically to build 178; this build hides an
  unfinished surface without changing its economy or native runtime.
- Existing v1.3 follow-ups attached to builds 175–177 remain active until the
  public store release is confirmed. `mote-machine-economy` remains deferred for
  meaningful device verification until the feature is redesigned and re-enabled.

## Release status

- Artifact: `build-178.ipa` (99,415,360 bytes; SHA-256
  `ce68d91508bb404248a8c561edbccfc39478a28bc4147235ab838cd39a6b9d48`)
- Signing inspection: passed identity, version/build, architecture, profile,
  entitlements, privacy-manifest, ZIP-integrity, and deep-signature checks
- Transporter: opened with `build-178.ipa`; Apple-ID sign-in and the final
  Deliver click belong to Brian, and no upload was initiated by Codex
- App Store Connect processing: pending
- Database: production live through `20260826040000`; zero pending migrations
- TestFlight device pass: pending with Brian
- Store release: not requested
