# Build 69 — 2026-05-22

## Headline

Cosmetic placement tuned. The anchor-relative position data for all
72 wearables is now populated — hats, glasses, held items, scarves,
auras sit where they should on the pig and scale across screen sizes.
A placement-data-only build on top of 68.

## User-facing

- **Cosmetics sit right** — every wearable now has a tuned
  anchor-relative position (pivot point, width fraction, which pig
  anchor, and whether it renders in front of or behind the pig).
  Items previously leaned on per-category absolute overlays that
  drifted on different screen sizes.

## Internal

- `constants/hat_rel.generated.ts` populated with 72 items via the
  `tools/item-anchor` web tool. Stale entries for the
  already-removed necklaces + capes were stripped (12 of them).
- No code or schema changes vs build 68 — placement data only.

## Migrations

None.

## Known issues

Carries forward build 68's known issues (Season Pass / Slop Club
dormant behind `IAP_ENABLED`, the 2 pre-existing `tsc` errors, the
new social surfaces not yet device-verified, Phase E on its own
branch). New:

- The `item-anchor` tool's browser localStorage still holds the
  removed necklace/cape keys — re-saving from an old session would
  re-introduce stale rows. Clear the tool's localStorage (or teach
  `serve.py` to filter) before the next placement pass.
- Populated placement data flips all 72 items onto the
  anchor-relative path — worth an on-device check that nothing
  regressed vs the old absolute overlays.
