# Build 96 — 2026-06-11

## Headline

The Mystery Hat Box finally contains a hat (with a reveal — and a retro-grant
for everyone who opened air), blessing/curse icons you can actually see, a
bigger daily shop with a dozen new items, the dead starter bundle is gone, the
Inbox tells you who did what newest-first, and a stack of audit fixes: popups
can never cover onboarding, the While-Away batch can't be eaten by a crash,
and the Trough finally talks back.

## Mystery Hat Box (tracker #6, server `20260631` ⚠️ push with this build)

- The tier-15 pass reward now grants ONE rarity-weighted hat you don't own
  (60/25/10/4/1); owns-everything falls back to +150 snouts.
- **Big reveal:** queue-disciplined modal — closed box → tap → your hat.
- **Retro-grant at push time:** everyone who already claimed tier 15 gets
  their hat immediately + a "Your Mystery Hat Box creaked open…" While-Away
  note (announcement row doubles as the replay guard). Watch the
  `RAISE NOTICE` count during db push.

## Ritual icon legibility

- Blessing/curse icons re-cut square (8 PNGs were on tall canvases) and now
  render in ink-outlined tinted wells across Barn chips, Inbox, Hoofprints,
  RitualPicker, and While-Away; tap targets ≥44pt. Known follow-up: 7 other
  emoji-art PNGs share the tall-canvas bug (tracked).

## Shop

- Daily rotation 5 → **8 items** (server `20260632` ⚠️ push with this build).
- **12 new catalog items** across hats/glasses/bows/held/auras/backgrounds/
  particles, costs inside the live rarity bands, emoji tiles until art lands.
- **Starter-pack bundle removed** — it was dead UI (no purchase path existed).

## Inbox

- Every What-happened row names who ("Jen answered your trade — +24 tickles"),
  sorted newest-first with a compact age, paginated 10 + "Load more".

## Audit & walkthrough fixes

- **Popups hold during gates:** nothing can present over sign-in / username /
  onboarding (`usePopupHold`) — the "stuck on Hi, I'm Rosie!" class.
- **While-Away marker on dismiss:** a wedge/force-quit can no longer eat your
  overnight blessings/curses; system-only batches persist the marker too
  (fresh installs stop skipping between-launch rituals). (#8)
- **Trough opening talks back (#7):** success toast with seed cost; real
  reasons on refusal (3-day cooldown / not enough snouts / ineligible).
- **Honest reward copy (#9):** claims say "+N snouts & score" everywhere —
  matching the 20260628 payout — incl. the funded announcement (server
  `20260633` ⚠️).
- Closet tile art no longer zoom-cropped (numeric insets; %-insets hit the
  same Yoga quirk as %-sizes). Regen chip + **linear alignment regen**
  (client mirror; server `20260630` ⚠️ — ship together, see below).
- Over-cap bank toast says "28 banked — regen resumes under 25". (#12)
- "Equip in Wardrobe" → "Equip it in the Closet".

## ⚠️ Push checklist (pending, in order)

`20260630` linear regen · `20260631` mystery box + retro-grant · `20260632`
shop expand · `20260633` announcement copy. **Push together with delivering
this build** — the linear-regen chip and the server are mirrors; either alone
makes the Me page lie in one direction.

## Build notes

`EAS_NO_VCS=1 COCOAPODS_DISABLE_STATS=true NODE_OPTIONS="--max-old-space-size=16384"
eas build --local --platform ios --profile production --non-interactive`.
Rename to `build-96.ipa`, upload via Transporter. Pre-ship smoke +
this build's specifics: claim tier 15 → reveal plays; daily shop shows 8;
Inbox names + Load more; Me page Regen shows a smooth %.
