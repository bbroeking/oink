# Build 171 — 2026-08-07 (v1.3, planned iOS buildNumber 171)

## Build record

- Classification: distributable build (not a release candidate)
- Release/version: 1.3
- Planned iOS build number: 171; EAS remote version is 170 before invocation
- Android version code: not applicable; iOS-only build
- Candidate commit: `e5aef0c911a60f4419bc0e7127df0410b7d54687`
- Working-tree state: clean detached worktree at the candidate commit (an
  ancestor of current `main`), plus this
  required untracked pre-build changelog. The primary development worktree's
  unfinished rewarded-ad, UI, native, research, and prototype changes are
  explicitly excluded from the binary.
- Previous verified artifact: build 170; build 171 supersedes it at the user's
  request with a fresh local archive from published source commit `e5aef0c`
- Previous live App Store build: v1.2, released 2026-07-14
- Release owner / rollback decision-maker: Brian Broeking
- Target: TestFlight via local production IPA and Transporter
- Planned release date: pending exact-binary TestFlight verification
- Database state required: production applied through
  `20260801010000_sounder_invite_security_grants.sql`; commuter-clock support
  is installed but `feeding_schedule` remains on legacy uniform mode until the
  exact compatible binary is uploaded and smoke-tested
- Feature flags/timed events: existing flags unchanged; after binary acceptance,
  activate the server-owned Eastern commuter schedule and smoke the clock/RPCs
- Deferred work: all uncommitted primary-worktree changes, rewarded-ad database
  migrations and Edge Function deployment, App Store release, and unrelated
  Homegrown Adventures prototype rollout
- RC status: not applicable
- Build result: succeeded locally; EAS remote iOS build number advanced to 171

## Headline

A fresh TestFlight build packages the merged commuter-clock and expanded
Sounder recruiting work from clean, published source.

## User-facing

- Sounder leaders can recruit from all-time truffle diggers, search by username,
  and offer consent-based Sounder switches.
- The client and server are ready for four server-owned Eastern Truffle Patch
  windows: 6–10am, 12–2pm, 5–8pm, and 9–11pm.
- The stray Generous-alignment white bubble fix is excluded because it remains
  uncommitted in the primary development workspace.

## Internal

- PR #52 and PR #53 are merged into `main` as commits `96f2213` and `458115a`.
- PR #52's unsafe phone-local migration remains superseded by the applied,
  server-authoritative `20260799000000_server_clock_commuter_windows.sql`.
- The newer current-`main` commit adds Homegrown Adventures documentation,
  art, Rive, and web prototype work; it does not alter the bundled mobile
  runtime and was intentionally excluded from the exact source snapshot.

## Migrations

- No new candidate migrations relative to build 170.
- Production is applied through `20260801010000`.
- Local rewarded-ad migrations `20260803000000` and `20260803010000` are outside
  this clean candidate and are not authorized for production.
- The commuter activation is a reviewed setting update, not a migration push.

## Known issues

- The public App Store remains on v1.2; TestFlight build 171 must be delivered
  and tested before commuter activation.
- Real-device core-loop/recruiting/clock smoke, notification paths,
  smallest/largest layouts, accessibility, offline/auth recovery,
  purchase/restore, links, camera paths, and Sentry inspection remain pending
  with Brian.
- Production permits Sentry symbol upload failure and excludes symbols, so
  crash symbolication may be reduced.
- App Store reviewer-account confirmation remains pending with Brian.

## Verification

- [x] Classification: distributable TestFlight build, not an RC
- [x] Candidate scope: clean published source at an ancestor of current `main`;
  unfinished dirty workspace explicitly excluded
- [x] PR state: #52 and #53 merged; merge commits present in candidate history
- [x] Remote database ledger: applied through `20260801010000`
- [x] Current production clock: legacy uniform mode
  (`window_secs=28800`, `open_secs=14400`, `offset_secs=0`)
- [x] App config: v1.3, `com.broeking.ttp`, production push, Sign in with Apple,
  `applinks:ticklethepig.com`, and required privacy strings
- [x] Production environment: privacy URL, Sentry, IAP, RevenueCat, symbols
  disabled, and Sentry upload allowed to fail
- [x] Rewarded ads are absent from this candidate, so no AdMob production App ID
  is required and no test ad ID can enter the binary
- [x] TypeScript: `npx tsc --noEmit`
- [x] Full Jest suite: 118 suites / 1,259 tests passed
- [x] Repository-wide Expo lint exception documented: 410 errors / 2,355
  warnings in pre-existing vendor, dashboard, Edge Function, test, and tool
  surfaces; candidate mobile runtime is unchanged from build 170
- [x] `npm run quality:check:full` constituent gates: contracts, layout,
  324-sprite integrity, security, TypeScript, Jest, production-source lint
  (0 errors / 138 warnings), simulator-free iOS export, and database harness
  passed. The wrapper exited only because the isolated worktree lacked ignored
  Supabase link metadata; the same linked-production lint was run separately
  and passed with warnings only.
- [x] Supabase dry-run boundary: remote database up to date, zero migrations,
  seeds, or roles queued from the clean candidate
- [x] Local production archive/export: succeeded with 17/17 Expo Doctor checks
  passing; Xcode archive and App Store export succeeded
- [x] Signed IPA inspection: ZIP integrity and strict deep signature passed;
  arm64, `com.broeking.ttp`, v1.3 (171), iOS 15.1+, production push,
  Sign in with Apple, associated domain, TestFlight beta entitlement,
  `get-task-allow=false`, no app extensions, provisioning expires 2027-03-20
- [ ] Complete real-device pre-ship checklist
- [ ] Exact TestFlight-installed binary verification
- [ ] Commuter activation and post-activation production smoke

## TestFlight “What to Test”

As a Sounder leader, open **Recruit any snout**. Confirm all-time truffle order,
username search, free-player invites, decline/cooldown, consent-based switching,
and block behavior. Before clock activation, confirm the current Patch countdown
still matches the legacy schedule. After activation, verify the server-owned
Eastern windows at 6–10am, 12–2pm, 5–8pm, and 9–11pm, including overnight
countdown behavior. Repeat Barn, shop/IAP, notification, link, camera,
offline/reconnect, sign-out/sign-in, and accessibility core smoke.

## Release status

- Artifact: `/Users/bbroeking/projects/oink/build-171.ipa` (91,877,635 bytes;
  SHA-256 `a9ff1ee4a6d14971efd6433aa04697c7d0b162f9b04428c15fd72ec704d3415d`)
- Transporter: pending; Apple-ID sign-in and final Deliver belong to Brian
- App Store Connect processing: pending
- Database: applied through `20260801010000`; commuter activation off
- TestFlight device pass: pending
- Store release: not requested
