# Build 151 — 2026-07-16 (v1.3, iOS buildNumber 151)

> Supersedes build 150 (delivered but review pulled). Same 1.3 headline
> content, plus two days of founder-session work: the feeding-clock fixes,
> the server-authoritative schedule, and Pair Keepsakes. DB state at build:
> migrations through `20260744100000` are PUSHED (pair bonds live + config
> row seeded at the LEGACY anchor — the 6-10/2-6/10-2 ET shift is a one-row
> UPDATE fired on release day; statement in the migration comment).

## Since build 150

### The feeding clocks are honest and self-healing

- Rollover bug fixed (founder repro: "dug — opens in 2h" still dug later):
  the dug flag is window-keyed (server-issued index) and expires by
  construction at rollover; AppState-foreground reconciles with the server;
  banner words + number derive together from the phase so "opens in" can
  never pair with a closes-countdown. 9-test phase×dug matrix.
- Banner footer states: open+dug → "dug this feeding — back next feeding ★";
  guarded → "… opens in {next-open}". Card pills countdown-free (the strip
  caption owns the schedule).

### The schedule is server-authoritative (config over constants)

- New `app_settings` + `app_setting()` RPC + `_feeding_sched()`; all four
  server window functions AND the client window math read the config row
  (client: fetch on launch/foreground, AsyncStorage cache, clamped, compiled
  fallback). Future schedule changes = one row UPDATE, no binary.
- Window math is offset-aware client+server (`floor((epoch−offset)/28800)`).
  Release-day flip moves everything together; harness rehearses the exact
  cutover (10_feeding_flip_smoke). 22 new client tests.
- What's-new gains "New feeding hours" — lands the same day as the flip.

### Pair Keepsakes + the Strongest Pairs board (server LIVE)

- `pair_bonds` ledger (trades/blessings/visits per unordered pair; curses +
  system rows excluded), trigger-maintained, backfilled from real history.
- Board tab gains a "Pairs" scope: "★ the strongest pair in the bog ★"
  champion, "{a} × {b}" rows with one number (bond), your best pair pinned.
- Friend sheet keepsake line: "you two: 12 trades · 8 blessings · 5 visits".

### Odds and ends

- Founding blurb no longer promises Sounder renaming.
- SeasonEndBeat removal + recruits count fix (were in 150, listed there).
- Rosie sticker-pack extension is BUILT but deliberately NOT in this binary
  (needs the `com.broeking.ttp.stickers` App ID registered with Apple) —
  the pbxproj target registration is set aside for this build and restored
  after; it ships in 1.4.

## Verification

- `npx tsc --noEmit` clean; Jest 44 suites / 561 tests green.
- db-harness: full chain green INCLUDING the release-day flip rehearsal
  (legacy seed verified → UPDATE applied → phase/coop/race/uniques smokes
  pass post-flip).
- Prod verified post-push: `app_setting('feeding_schedule')` returns the
  legacy anchor (offset 0) via anon RPC — live clients unaffected.

## Numbering

- CFBundleVersion 151 via EAS remote auto-increment. Marketing version 1.3.
  **Deliver build-151.ipa only**; 150 is superseded (pull 1.3 from review if
  already submitted, then resubmit with 151).

## Release-day runbook (after 1.3 is approved)

1. Release 1.3 in ASC.
2. Fire the flip (SQL console or ride-along migration):
   `UPDATE public.app_settings SET value = jsonb_set(value, '{offset_secs}', to_jsonb(7200)) WHERE key = 'feeding_schedule';`
3. Clocks move everywhere within one app launch; what's-new tells the story.
