# Build 107 — 2026-07-11 (v1.1, iOS buildNumber ~132 via EAS auto-increment)

> The Season-1 flip binary. Ships with in-app release notes **1.9.0 "The
> Truffle Patch runs deeper"**. The season itself flips LATER via the two
> `app_config` flags (`season1_finale`, `world_boss`) + the held finale-stack
> migrations (20260726/27) — this build just carries everything those need.

## The Truffle Patch, rebuilt into a fossil-dig loop

- **Fixed the broken board**: the 6×5 grid wrapped into 5×6 with a dead column
  (floored tile widths vs the border box); taps mapped to the wrong tile.
  Explicit flex rows + shared pure hit-test math (`tileIndexAt`).
- **Choose-your-dig boards**: depth tuned to ~60–70% clearable (simulation-
  pinned test); uniform mystery silhouettes; crack marks on partial digs;
  shove hold telegraph.
- **Cluster-legible truffles**: pieces render half-buried under a mud mound;
  ONE big truffle pops when a cluster completes; "N of 2 truffles dug".
- **UNIQUES + the Burrow Book**: 12 relics (real generated art), server-rolled
  ~2-in-5 boards (migration 20260728, PUSHED), dex page with dark silhouettes,
  found-counts, end-card entry line.
- **Dig SFX**: ambience bed + scrape/creak/truffle-pop/shimmer/clink.
- **Real stone sprite** (the old drawn pebble read as a UI glitch).
- Dormant in this binary: carry-over gild (migration 20260730 HELD).

## Golden Tickets (QR redemption) — migration 20260732 PUSHED

- `redeem_code` RPC: normalized `PIG-XXXX-XXXX` codes, per-user-once, race-safe
  uses, zero-policy RLS, truffle grants via the audited mint ledger, pre-write
  refusals (unknown/expired/exhausted/already_redeemed/pouch_full/bad_grant).
- Scanner screen (`/scan-code`): expo-camera (NEW NATIVE DEP — pods rebuilt,
  NSCameraUsageDescription added), graceful no-camera/typed-entry path, named
  claim reveal. Entry: gift chip icon in the shop header.
- **Release Party Crown** seeded (20260733, PUSHED): legendary, cost 0,
  grant-only. ⚠️ Needs a placement-studio pass before granting.
- Deep link: `ticklethepig.com/redeem/<code>` (QR-scannable with the OS camera).

## Dig-Off + season surfaces

- **Full-field standings page** (`/race-standings`): every Sounder, 25 at a
  time, my crew pinned in a sticky card, season/weekly toggle, member ledgers.
- Dig-Off card refreshes on `digTick` (was stale under the dig modal).
- **Login liturgy**: Great Hunger intro queue-integrated at priority 27 (root),
  canonical flip order verdicts → recap → intro → while-away → sounder nudge;
  ceremonies suppress achievements/release-notes for the session.
- Season-end walkthrough: real Rosie sprite, reward showcase cards, legible
  snout count, safe-area Skip/card anchoring.
- Copy: the 4h-open/4h-guarded feeding rhythm taught in story + guide.

## Invite links

- `ticklethepig.com/i/<CODE>` universal links (landing DEPLOYED, AASA updated,
  `/r/` legacy kept — and its prod 404 fixed); share message carries the link;
  paste-code affordance at the referral gate.

## Housekeeping

- All three purchase CTAs read "Coming soon…" behind `PURCHASES_LIVE`.
- Flowers charm removed from the Me-page avatar; Rewards/Exchange are real
  buttons; SpoilsShowcase/SeasonStory/SeasonInfo layout fixes.
- DEV-only: `dev · practice dig ›` (any-time practice board, mints nothing).

## Ship steps

1. `NODE_OPTIONS="--max-old-space-size=16384" eas build --local --platform ios --profile production`
2. Rename artifact → `build-107.ipa`, `open -a Transporter build-107.ipa`
3. Post-approval, at party time: push 20260726+27 (`--include-all`), flip
   `season1_finale` + `world_boss`, mint the party code (crown grant), print QR.
