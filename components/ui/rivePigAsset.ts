/**
 * Production gate. Replace with a static require only after the recovered rig
 * passes the binary, playback, six-coat, and attachment gates. Metro must never
 * be pointed at a generic runtime sample or the adventure scene as Rosie.
 */
export const RIVE_PIG_SOURCE: number | undefined = undefined;
