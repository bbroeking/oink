---
name: tickle-the-pig
description: "The product charter for Tickle the Pig. Consult before any product, design, content, or economy decision and weigh the choice against the pillars and the lens. A living document — append important decisions to the log. Rewritten 2026-07-07 to match the game as it actually is; the previous charter and its decision log are preserved in git history."
---

# Tickle the Pig — what we're building, and what we believe

## North Star

**Tickle the Pig connects friends into herds, gives them things to collect, and gives the herd a race to run — together.**

Connect · Collect · Contend. One sentence; everything serves it. When a choice doesn't, we change the choice — not the sentence.

## The three pillars

- **Connect friends.** The game is a reason to reach out. Visiting a friend's Barn, blessing them, digging the same feeding, rallying the herd before Thursday's bell — a feature earns its place when it gives one player a warm reason to think about another. Rivalry counts: "we're two finds behind" is a text message.
- **Collect things.** Cosmetics, titles, truffles, a happy pig. Everything with status attached is **earned through play, never bought** — money buys expression, never advantage or accomplishment. Collecting is self-expression and proof you were there.
- **Contend as a herd.** The competitive unit is the Sounder, never the lone pig against a friend's pig. The season board accumulates — every find counts forever *within the season* — and resets when the world's story does; the weekly spoils race keeps every Monday winnable by anyone. No standing outlives a season. Competition exists to make cooperation urgent — you dig *together* or you place nowhere.

## The decision lens

Before building anything, answer:

1. **Which pillar does this serve?** If none, push back before writing code.
2. **Can a player hold it in one sentence?** If the mechanic needs two explanations, it's wrong — cut layers until the sentence survives. (This lens killed the fronts board, the mud unit, the fixture league, and the 1-v-1 duel. It will kill again.)
3. **Is it fair by construction?** Scoring per digging snout, participation-gated rewards, server-side minting, quorums that make ghost rosters self-defeating. Fairness added afterward is a patch; fairness by construction is a design.
4. **Does losing still feel warm?** Losers keep everything they earned; sleeping pigs never drag their herd down; sub-quorum herds are nudged, never named and shamed; there is no decline button, no public zero, no ladder to fall down.
5. **Can the reward pipeline actually sustain it?** A reward cadence the art pipeline can't feed becomes a broken promise on a schedule. Queues with graceful fallbacks, or don't ship it.

## What we believe

- **Legibility beats depth.** A week of design taught us this the expensive way: five clever layers compound into noise. One legible loop, then earn each addition.
- **Competition is social fuel, not a hierarchy.** Twice a week every herd runs the same race; Monday it's forgotten. Rankings live on their own card — never on Rosie, never on the Barn.
- **Earned over bought.** Titles, milestones, podium art: play only. The economy's mints are closed, audited, and server-side.
- **The world responds now, and speaks in pictures.** The Hungerer weakens visibly; moods are sprites; a find names itself the moment it surfaces. Numbers are honest and small — a find is a find, not a thousand of anything.
- **Rendezvous over grind.** 8-hour feedings, three-day races, notification-based moments ("the race is run") — the game calls you at human intervals and never punishes the hours between.
- **Craft is belief.** No emoji in UI; technical names in code, cozy names on screen; tokens over inline values; footguns get comments.

## Anti-patterns we refuse

- Mechanics that need explaining twice — to a player *or to their own designer*.
- Standings that outlive a season; weekly rewards a herd can't win by simply showing up strong this week.
- Shame states: drag-down averages, public zeros, rejection buttons, "your herd let you down" framing.
- Pay-to-win in any costume; status you can buy; reward faucets without sinks.
- Reward cadences that outrun the art pipeline.
- Scoring chains more than two nouns long (the cautionary tale: dig → mud → rope → fixture → ribbons).

## How to use this document

- **Every session, reflect.** Hold each proposed change against the North Star and the lens *before* building it.
- **Append important product, design, content, or economy decisions to the log below** — what we chose and which pillar it serves. Dated, newest-first, short.
- The pre-2026-07-07 charter and its decision log (Seasons 0–1 archaeology: alignment, Judgement Day, mud wars, the league, the co-op teardown, the duel) live in git history and on the `archive/sounder-league-2026-07-06` branch.

## Decision log

- **2026-09-16 — The Shop is a storefront; the Trough lives in the yard; a chip past a notch drops tickles.** Founder chose direction C (The Storefront) from three front-page directions (`docs/design/claude-design/shop-2026-09-16/`), with two pieces of B folded in: the sounder's open Troughs sit in a wooden trough by the counter as notched rows, and friends who bought today stand at the counter wearing it (a friend who bought nothing is simply absent — never a public zero). On Home, an open sounder Trough is a small trough in the yard beside the mound (a thing in the yard, under the 2026-09-13 rule) and a `Trough` row on the Barn button's fan; both open the Trough sheet. The donor reward changes shape, twice in one day: the pot rule (a notch spills a share of a cost/100 pot) was drawn and balanced (`tools/balance_trough_notch.py`), then **superseded by the quarter rule** — a giver earns from a Trough only once their contributions reach 25% of the price, and the first quarter reached in a week draws **one random reward** (a furnishing from the featured collection at 60/28/12, or a tickle purse when the collection is owned); nothing more that week, however many Troughs or snouts. The 1-per-100 credit retires. Same call: **the Barn Draw** — every Monday each crew (up to 8, crew roster only, never the friends list) draws one for-sale furnishing among its diggers, equal odds, seed committed before the week ends and revealed with the result; collection gifts, keepsakes, pass rewards and bunting are never drawn. This lifts the furnishing expansion's recorded "no random rewards" rule for for-sale designs only. Why: a quarter-then-one-draw cannot be farmed (snouts convert to nothing at any rate) and the crew draw keeps scarcity at the size of the competitive unit (a regular digger wins about monthly; game-wide it would be years). Migration `20260916120000_barn_draw_and_trough_quarter_reward.sql` (written, not pushed). Serves **Connect** (the shop is the tab where your herd is visible; chipping in is a small surprise, not a rate) and **Collect**. Build order: Home first, then the store's chrome, then the painted scene (grid stays as the fallback).
- **2026-09-16 — The members' shelf rotates like the drop; the Pen hangs as a third sign.** Building the storefront's chrome (build 2) settled two things the canvas left open. (1) `daily_shop()` excludes members-only pieces, and the Members band that used to sell them went with Collectibles when it merged into the Closet — so the Slop Club shelf is the one place the members catalog is on sale, and it stocks **three pieces a day**, picked from the id and the UTC date (`utils/shopShelves.ts`) so every member in a sounder sees the same shelf and it rolls with the drop at midnight; a non-member sees them under the gold lock and the sign is a door to the Pen. Not the whole catalog: a members' shelf should have the drop's rhythm, not a browse page's. **Pass rewards never rotate in**: the nine members-only cosmetics on the season-1 premium track (20260727) carry catalog prices but are `pass_exclusive`, and the shelf picks only from for-sale pieces (`members_only`, not `pass_exclusive`, cost > 0) — owned or not. A battle-pass piece is the pass's; the shelf sells beside it, never instead of it. (2) The Pen (Account and the paywall deep-link to it) hangs beside Closet and Furnish rather than losing its door, and a `Store` sign takes the chalkboard's place while you are in the Closet or the Pen. Serves **Collect** (the members catalog is on sale again, on the shelf's terms) and **Connect** (the Pen stays one tap from the store).

- **2026-09-16 — Monday pays furnishings to the herd and a purse to each snout.** The weekly race's herd tickle payout (2026-07-27's 500/300/200/100/50 ladder) is **retired**; the cycle now grants **Barn Bunting** to every digging snout in a ranked herd and **Gold Bunting** on top to the winning herd's diggers (`grant_habitat_item`, idempotent per cycle; Golden Truffles unchanged; `race_standings().prizes` reads honest zeros for tickles and names the two furnishings). In its place every snout that dug at least one feeding that week draws its **own Monday purse** — 20 common / 60 good / 150 rare / 400 jackpot at 60/28/10/2 (placeholders in `app_settings.monday_draw`), rolled and minted server-side through `apply_tickles`, one per ISO week, drawable all the following week so a Wednesday opener isn't punished. **Catch-up is a soft pity that only ever warms**: each consecutive Monday below rare raises the rare+jackpot share (×1.5 after two, ×2 after three, capped ×3), a herd that finished in the bottom half adds one step, and a week without a dig is simply absent — it neither counts nor resets. The sheet says the streak as a promise (*Four Mondays without a rare, so next Monday's warmer: 1 in 3*) and a slept week is a door (*Dig any feeding and next one's yours.*), never a verdict. Chosen because a herd's rank is a shared number and its spoils should be a shared, keepable thing in the Barn, while the per-snout variance a jackpot needs belongs to the individual — so the race stops being able to drag a snout's tickles down and nobody in a small herd is a public zero. Serves **Contend** (Monday keeps a beat anyone can take), **Collect** (spoils hang in the Barn, season over or not) and **Connect** (bunting is the herd's, and it is never named who slept). Migration `20260916110000_monday_tickle_draw.sql` (authored, harness-validated, unpushed).

- **2026-09-11 — The Great Hunger is extended a second month, to 2026-10-12 00:20 UTC.** The 2026-08-12 one-month extension lapsed at 00:20 UTC on 2026-09-12 with the Hungerer only at Peckish (4,083 drained), which left `active_season()` empty and every dig, claim and standings surface reporting "no season". Not a stop — a cutoff nobody moved. Migration `20260912020000_extend_great_hunger_season_again.sql` (pushed, verified: `season_state().active = true`) uses the same never-shorten guard; the next season's migration supersedes it. Founder call: "bring the current season back." Serves **Contend** (the race and the meter keep running) and **Connect** (no herd loses a week to a calendar).

- **2026-09-11 — Two route rulings from the wave-4 polish pass: `/sounder` belongs to the crew, and the Lounge is dark-launched rather than dead.** (a) The referral board and its reward ladder move from `/sounder` + `/sounder-progress` to **`/recruits` + `/recruits-progress`**. The file's own header already said the surface can't wear the word "Sounder" (it was reclaimed for the crew), but the route did — while the Friends hub's `?seg=sounder` meant the crew, so a deep link to `/sounder` landed on referrals (finding B-15). One concept, one name, in code as well as on screen; the glossary entry is in `CONTEXT.md` so it cannot re-collide. Serves **Connect** (the crew's word stays the crew's) and craft. (b) `app/lounge.tsx` rendered an unconditional `<Redirect>` above 850 lines of working screen — the Lounge is unfinished, not retired, so the redirect now gates on **`LOUNGE_VISIBLE = false`** (`constants/featureFlags.ts`, dated, with the reason on its face), and flipping one constant opens the field. Serves **Connect** — the Lounge exists so members can be in one place at the same time; it ships when it reads as ours, the same bar the Mote Machine was held to. Companion polish in the same pass: the Season scroll's one-hero rule now reaches every card it composes (`utils/seasonHero.ts` elects exactly one surface per state — dig → the Feeding card, claim → the claim bar, join → the Sounder door, browse → the Hunger banner), and a portrait-plus-name on the recruits board and the Porch Round opens `UserSheet` instead of swallowing the tap (B-16) — **Connect** again: the scrapbook of who you visited is now a way to visit them back.

- **2026-09-11 — The Mote Machine goes dark until it is rebuilt on the design system.** The UI/UX audit found `MoteWageringScreen` is the one live surface an unrelated product could ship unchanged (design-specificity FAIL, finding D-06), so `MOTE_MACHINE_VISIBLE` flips to false: entry points, the season card, the digging-stats rows, and the direct route all hide; Motes keep accruing server-side for the relaunch. Serves **Collect** (the reward surface returns only when it reads as ours) and **craft is belief** (a surface that isn't in the sticker language isn't shipped). Companion: the full audit + Design System v1 in `docs/design/audit-2026-09/` and `docs/design/design-system-spec.md`.

- **2026-09-08 — Barn navigation uses clear actions in the existing design system.** Home leads with an illustrated Enter Barn button; the room labels Home, Furnishings, and Decorate visibly. Shared buttons, type and spacing make collecting and visiting easier to discover without adding a new navigation layer. Serves **Connect** and **Collect**.

- **2026-08-12 — Farm Life and Beyond the Hedge become separate game lanes and separate validation surfaces.** An Adventure now starts with a nearby possibility and stands on destination, Tool, Pack, Intention, trip shape, return story, and a lasting Home trace; crops, plots, harvest timing, compost, and Farm stock do not unlock, provision, accelerate, or explain it. The dedicated `ticklethepig.com/adventures` click-through uses an illustrative fixed **+20 tickles** on homecoming so the loop returns to the game's central currency without making the “best” preparation a payout calculation; that amount is prototype tuning, not production economy tuning. The combined Homegrown Adventures lab remains historical integration evidence, while the standalone click-through is the canonical external Adventure test. Serves **Collect** (named Finds and remembered places), **Connect** (the return creates something worth sharing), and legibility (one Adventure sentence survives without the Farm loop attached).

- **2026-07-27 — The Truffle Patch names its reward before the tap and itemizes it after the dig.** Home and Season now lead with **Dig for Golden Truffles**, state the guaranteed **+20 Pass XP**, and explain that bringing home a find qualifies the pig for the active Hunger stage's **15-Golden-Truffle reward** and the Sounder's Monday Dig-Off spoils. The receipt repeats those outcomes beside the actual finds, so the player never has to infer why digging matters from three separate downstream screens. Serves **Collect** (the personal reward is explicit), **Contend** (the shared consequence is explicit), and legibility (one action names its immediate and shared payoff).

- **2026-07-27 — Signing a Barn guestbook is an optional in-visit action, never a first-tickle interruption.** A successful first tickle quietly unlocks **Sign the guestbook** beneath the shared-heart tally; the visit keeps flowing until the player chooses it, and dismissing the sheet leaves the action available. Serves **Connect** by preserving the warm keepsake without making social expression compulsory, and legibility by keeping tickling as the visit's uninterrupted main loop.

- **2026-07-27 — The Barn collapses live updates; it does not “clear” live game state.** The permanent tickle tickets remain the HUD. Temporary effects and the recurring Truffle Patch action now share one collapsed-by-default **Barn updates** tray, with their names still visible in its summary and their full controls one tap away. Active effects and feeding state cannot be cleared because they still affect play; truly optional prompts retain their own dismiss action. Serves **Collect** + **Contend** by keeping the next meaningful actions reachable without letting status chrome push Rosie out of the scene, and legibility by giving the player one sentence: “Tap Barn updates when you want the details.”

- **2026-07-27 — Weekly Dig-Off tickle spoils become a Monday jackpot, and the card names your projected payout.** The per-digger ladder becomes **1st 500 · 2nd 300 · 3rd 200 · top half 100 · field 50**, with a **25-tickle participation floor**. That makes first place worth twenty normal free-player refills while every digging pig earns at least one. Golden Truffles and find drops stay unchanged: early churn is the larger risk, while the spendable-tickle ladder is reversible server tuning. A ranked Sounder now sees the tickles and truffles it is currently on track to bank, connecting the next dig to Monday's reward instead of presenting a distant static pot. Serves **Contend** (the weekly win matters and the next action is legible) + **Connect** (each pig can see what its shared Sounder position earns). Migration `20260793000000_major_race_tickle_spoils.sql` (authored, unpushed).

- **2026-07-27 — Crewed players dig the Truffle Patch directly from the Barn.** The Great Hunger home chip is now the feeding action itself: **Dig now** opens the patch in place, while guarded and already-dug states keep the same slot with an honest countdown/status. Crewless players still get the practice/join path. Home and Season reconcile the shared server truth whenever either tab receives focus, so two mounted entry points cannot keep disagreeing after a dig. Serves **Collect** (the collection loop is one tap from the home habit), **Contend** (every feeding is easier to join), and legibility (the player can hold the loop in one sentence: “When Home says Dig now, tap it and dig.”).

- **2026-07-25 — The Pen becomes the Slop Club’s companion hook, and Trough rewards stop dangling.** Rosie’s five visible friends now live in **The Pen** inside Shop: everyone can preview the real animated pigs beside Rosie, while members get one replaceable companion slot and choose who greets them at Home from the Pen—not from Home itself. The old fourth Trough tab becomes a conditional collapsed row above Shop navigation, showing only when the player has active Troughs or updates. Because the revived 1:100 donor reward already credits inline, all existing pending donation receipts are stamped claimed and future rows default claimed, removing the impossible manual-claim state without double-paying. Serves **Collect** (the pigs are the membership’s tangible collectible draw) + **Connect** (Rosie gains a friend; Trough help stays visible without owning a whole shop section). Migration `20260782000000_auto_claim_trough_rewards.sql` (authored, unpushed).

- **2026-07-22 — The Great Hunger pays 15 Golden Truffles at every server stage transition, and reward pushes never interrupt foreground play.** The Season ladder matches `hunger_meter()` one-to-one: Gorged → Stuffed → Full → Peckish → Hungry → Famished. Contributors share **15 Golden Truffles** at each of the five transitions. Eligibility is stage-specific and participation-gated: a pig must bank at least one credited find during the stage being completed; grants are server-minted, cap-aware, ledgered, and idempotent. Reward notification is durable in-app plus best-effort push; foreground pushes are quiet unless a producer explicitly opts into `foreground: alert`, preventing a dig from sounding its own reward over the receipt. Backfilled rewards deliberately do not create a deployment-time push blast. Serves **Collect** (an honest shared truffle reward) + **Contend** (the global meter now has tangible communal beats) + legibility (server and UI share one stage model). Migration `20260775000000_hunger_stage_rewards.sql` (authored, unpushed).

- **2026-07-21 — Referrals complete at 100 tickles and pay 100 tickles.** Founder simplification: entering a valid code still gives the new player **50 snouts immediately**; the referral then completes on that player's **100th lifetime tickle**, with no three-active-day requirement, and pays the inviter **100 spendable tickles** instead of 100 snouts. The milestone ladder remains unchanged. Existing qualified referrals waiting only on the retired day gate are backfilled through the same idempotent completion helper. Serves **Connect** (the invitation payoff arrives on one understandable mark) + legibility (one threshold, one plainly named reward). Migration `20260772000000_referral_tickle_reward.sql` (authored, unpushed).

> Each entry: date — the decision — which pillar(s) it serves and why.

- **2026-07-21 — The Wallow turns a completed pass into major regeneration ranks and a sparse repeatable path, not a tickle payout.** Founder amendment: Wallow Rank 1 makes tickles **25% faster** and Rank 2 reaches **+50%**; clock power caps there while public rank keeps climbing. The UI states the before/after rank, percentage, and exact effective `1 tickle / Xm` interval. After the first Wallow, the dense Free/Premium pass becomes one earn-only **Prestige Path** with five rewards across 30 tiers (125 snouts · 2 Golden Truffles · Mystery Hat Box · 250 snouts · 2 Golden Truffles); tier 30's headline reward is the next rank. Claims reset per lap and there is no paid prestige lane. Rosie still powers up visually through five earned gold/fire stages with Reduce Motion; leaderboard pigs show a gold/fire W-rank frame plus explicit rank and regeneration strength. Rooting activity — burying snouts, digging friends' pots, and Feeding-window Golden Truffle Patch digs — feeds the XP path. Serves **Collect** (permanent earned status + sustainable repeat rewards), **Connect** (social digging routes into it), and legibility (rank and tickle clock are stated directly). Migration `20260769000000_prestige_wallow.sql` (authored, unpushed).

- **2026-07-21 — The Dig-Off opens weekly, keeps every settled week, and holds a separate season view.** Founder call: the full Dig-Off page now has three lenses in time order — **This Week**, **Past Weeks**, **Season**. Every lens can swap its visible score between **Per snout** and **Overall**, remembering the useful default per lens (live = Per snout; archive + season = Overall), while official ranks stay fixed to the rule that awarded them (weekly per-snout rank; season total-finds rank). Past Weeks opens on last week's complete table with older/newer navigation; the archive keeps each final weekly rank but defaults to total finds because live competition needs the fairness score while memory needs the herd's tangible haul. It keeps the whole field, including sub-quorum Sounders, but never calls anyone a loser; losing still keeps its story without becoming a shame state. History is read lazily from the season-scoped `race_digs` ledger through `race_history()`, and historical rows deliberately do not open the live member ledger (it cannot honestly reconstruct old rosters). Serves **Contend** (the weekly race is legible now and remembered later) + **Connect** (a Sounder's shared weeks become history) without muddying the always-winnable Monday reset. Migration `20260768000000_race_weekly_history.sql` (authored, unpushed).

- **2026-07-21 — Weekly spoils gain tickle rewards for every digging Sounder (top-heavy), and the weekly board becomes the hero.** Founder call (spec 21): the Dig-Off's weekly race now pays **tickles** on top of its Golden-Truffle + podium-hat spoils — **1st 50 · 2nd 30 · 3rd 20**, ranked field **12/8**, and a **participation floor of 5** (juiced same-day from 25/15/10·6/4·3 — founder: make the win feel like a jackpot) for every digging snout in a *sub-quorum* crew (dug ≥1 but under the 2-digger quorum). Everyone who dug takes a share; the podium takes visibly more; the numbers are **server-tunable** (`_race_tickles_for_rank`, surfaced to the client via `race_standings().prizes` with a compiled fallback — the server-config-over-constants rule). **Tiebreak honesty is absolute:** the grants go to the **spendable tap pool** via `grant_tickles()` (`user_items.item_count`), **never `tickles_earned`** — adding granted tickles to the season's Most-Tickles tiebreak would repeat exactly the pollution the spec-15 clawback removed. Presentation flips too: the **weekly board is promoted to the hero** (top, biggest, with a prominent this-week's-spoils strip showing the ladder) and the **season-cumulative board drops to a collapsible secondary card** — the winnable-every-Monday race reads first, the season-long lens stays always-visible but quiet. No-shame preserved: sub-quorum crews still get no race-end push, but their diggers now get a warm in-app "your digging counted" note (a reward, not a placed-nowhere zero). Serves **Contend** (the beat anyone can take, with obvious prizing) + **Connect** (a small herd that dug together is never a public zero). Migration `20260767000000_race_tickle_spoils.sql` (authored, unpushed; carries `_race_pay_cycle` from 20260719 + `race_standings` from 20260720, verbatim). Spec 21.

- **2026-07-20 — Slop Club includes the season pass; the standalone Season Pass product stays dormant.** Ratifying where the implementation deliberately landed (migration `20260686` reversed the original two-independent-products spec): membership (`is_vip`) unlocks the premium track at read time (`season_state`/`claim_tier_reward` honor it), the premium banner sells the subscription, and the one-time `season_pass` consumable — review-approved and configured in ASC/RevenueCat — ships with **no buy surface** until a deliberate later enable. Guardrails locked in today: `season_pass` must NEVER map to the `tickle_the_pig_pro` entitlement in RevenueCat (a pass buyer must not get the whole membership), and the webhook now grants/revokes the pass's premium track directly (service-role write; `grant_season_pass()` reads `auth.uid()` and is unusable from the webhook). Also ratified: the monthly stipend is a **manual claim on the membership card** (a small ritual that brings members back to the scrapbook, vs. the spec's silent on-launch grant), and the 2× regen perk joins the card's pitch (it was live but unadvertised). Serves **Collect** (membership = the collector's lane: members band, stipend, drops) + honesty (one product does what it says; no accidental entitlement bleed). Spec addendum in `docs/pass-and-slop-club-spec.md`.

- **2026-07-20 — The Trough tickle reward comes back, at 1 per 100 snouts.** Founder call amending the 2026-07-17 spec-15 "zero forever" stance: donations pay the donor **1 tickle per 100 snouts spent** (floor, on the snouts actually taken after the quarter cap + remaining-target clamps), credited **immediately** at donation time. The rate history is now 10:1 (`20260628`) → 0 (spec 15) → **1:100 (today)**. The claim lane stays retired (`claim_drive_reward` still refuses; the credit is applied inline in `donate_to_drive`), and spec 15's retroactive clawback of the old 10:1-era grants is **unchanged**. Serves **Connect** — the herd gets a small thank-you for chipping in without re-inflating the board: at the quarter cap a fully-communal 2,000-snout Trough mints **≤20 tickles TOTAL across its ≥4 donors**, so `tickles_earned` stays honest as a tiebreak while generosity earns a token nod. Migration `20260757000000_trough_quarter_cap_and_72h.sql` (edited on `workflow-cleanup`, unpushed — the reward revival rides the same file as the quarter cap + 72h window).

- **2026-07-20 — The Trough gets a quarter cap and a three-day window.** Founder call: two rules on friend-funded item drives. (1) **No pig fills more than a quarter** — cumulative per-donor contributions to a Trough are capped at 25% of its target (`ceil(target*0.25)`); a donor's chip is clamped to their remaining headroom, and a donor with none left is refused (`donor_cap`) before any charge. The opener is a pig too, so the cap covers their seed as well — but their seed is *clamped* down, not refused (it's their own Trough opening; friendlier than a wall — and the client only ever seeds 10%, so it never bites in practice). (2) **Troughs stay open 3 days** — the window grows 48h → 72h; the existing refund machinery (`resolve_expired_drives`, per-donor SUM refund + seed refund) keys off `closes_at` and carries the change with no edit. Serves **Connect** — a filled Trough is now the herd's doing, not one whale's: at the cap it takes at least four pigs to fill one, so generosity stays shared and legible. Migration `20260757000000_trough_quarter_cap_and_72h.sql` (authored on `workflow-cleanup`, unpushed; carries donate_to_drive from the spec-15 retire base and open_item_drive from the in-shop-gate base). Client folds the headroom into every chip amount so no button ever offers more than the server will take.

- **2026-07-17 — A "Max" button lands on burying snouts.** Founder call: the buried-truffle stake chips change from [10][20][50] to **[10][20][Max]** — the 50 chip is replaced by Max, which fills the pot to the 50-snout cap bounded by the host's balance (fresh bury: `min(balance, 50)`, valid ≥ 10; top-up: `min(balance, 50 − remaining)`, valid ≥ 1). The chip shows "Max"; the confirm button restates the concrete number (one source of truth). Serves **Connect** — friction-free generosity: a host tops the shared pot to the brim in one tap instead of arithmetic. The four economy governors are untouched: the 50-snout pot cap, one active pot per host, the 3h dig cadence, and the 12h reclaim cooldown. (Server whitelist relaxed to a range in `20260756000000_bury_truffle_max_amount.sql`, authored/unpushed; new clients handle the pre-push `bad_amount` gracefully so ship order is harmless.)

- **2026-07-17 — The tickle breakdown: the board's number becomes a glass box.** Founder calls: any player's season tickles decompose into a receipt-style sheet (home taps · visits · digs · season pass · trades · lucky), reachable from leaderboard rows, UserSheet, and your own count — self and others render identically, **full honest ledger** including the season-pass lumps, before the pass-tier tiebreak policy is even decided. Why: the coopatroopa69 audit showed the "Most Tickles" number quietly blends played and granted; transparency first, policy second — if pass lumps feel wrong, the community will show us. Serves **Contend** (a kind leaderboard is a legible one). Spec 17.

- **2026-07-17 — The Field Guide: the economy becomes a thing you collect knowledge about.** Founder-grilled decisions: it's a **discovery journal, not a manual** (Collect pillar — the Burrow Book pattern applied to the economy itself); **economy objects only** (feeling-surfaces stay show-don't-tell per the taste law); a **second shelf beside the Burrow Book** on the dig-collection screen; **silhouettes until first encounter, full ceremony reveal on unlock** (founder chose the Book-style reveal over a quiet toast); **real numbers, config-fed** (value lines read server tuning with compiled fallback — the guide can never lie after a rebalance). V1 shelf (8): Truffle, Golden Truffle, Lucky Number, Trough, Mud Wrap & Warm Tea, Snouts, the Exchange, Feeding Windows. **Echo deliberately cut from v1** — the crew-echo rework is pending and an entry now would describe a mechanic about to change. Serves **Collect** (pages are earned) + legibility (the "what does this even do?" gap the lucky-number invisibility exposed). Spec 16.

- **2026-07-17 — Troughs stop paying tickles; this season's trough tickles are removed.** Founder call: the donor tickle reward (10 snouts → 1 claimable tickle on a funded Trough) is retired entirely, and tickles already granted for troughs completed since the season-1 start (2026-07-12 finale reset) are clawed back from both the bank and `tickles_earned`. Why: `tickles_earned` is the season's Most-Tickles tiebreak — trough grants let snouts buy tiebreak position, polluting a number the finale treats as earned-by-tickling; and the Trough's real reward was always the friend getting the item. Serves **Contend** (the tiebreak stays honest) + **Connect** (a Trough is generosity, not an exchange rate). Migration authored on `workflow-cleanup` (spec 15), rides the next push batch.

- **2026-08-29 — Visit Streaks replace Pair Flames.** Founder call: the flame belongs to an unordered friend pair, and either pig's first qualifying Barn visit keeps it alive. It is visit-specific rather than a broad mutual-action checklist; a reciprocal visit on the same streak day does not add a second credit. The live count stays explicit on friend rows, the longest flame remains banked, and a miss sleeps without blame. This supersedes the 2026-07-15/16 Pair Flame trigger rules while preserving their art and matching milestone-cosmetic direction. Serves **Connect** through a relationship with a face and gives either friend agency to keep the ritual warm. UI brief: `docs/design/visit-streaks-ui.md`.

- **2026-07-16 — The Hearth Archive is rejected; Pair Flames stay a streak.** Founder call against both standing recommendations: the proposed redesign (lifetime reciprocal days, non-expiring, never resets) is out. The 07-15 model stands as ratified — flames climb per consecutive mutual day, sleep to a wisp on a miss with the count banked to "longest," and rekindle fresh. The ritual pull of a live run (something that climbs *because you both kept showing up*) is the point; lifetime accumulation already exists as Pair Keepsakes, and a second cumulative number would make the flame redundant. Serves **Connect** — the flame is the appointment, the keepsake is the memory. Superseded by the 2026-08-29 Visit Streak decision above.

- **2026-07-16 — Mud-wrap stacking: overlapping wraps extend duration, never multiply regen.** Option 1 as proposed — re-wrapping a still-wrapped pig adds time to the clock instead of stacking the regen multiplier (no compounding buff economy, no "wrap-bombing" a friend into a regen exploit). Serves **Connect** (generosity stays generous — a second friend's wrap is never wasted) while keeping the economy honest. Migration authored in the workflow-cleanup batch; rides the next push on founder go. Founder call: window boundaries shift +2h (UTC anchors 02/10/18), so the patch opens **6–10am, 2–6pm, and 10pm–2am Eastern** — morning commute, homecoming, wind-down. Explicitly traded away: the 8–10pm prime-time block (guarded 6–10pm ET). Anchor stays UTC, so the wall-clock schedule slides an hour at US DST changes — accepted. Ships in a resubmitted 1.3 (build 151) so launch players never feel a mid-season rhythm change; pre-shift clients desync until they update (tiny beta, accepted). Serves **Contend** (feedings land where real days have gaps, so the herd actually shows up together). Migration `20260744000000_window_shift.sql`.

- **2026-07-15 — Streaks enter the game as Pair Flames — mutual, banked, blame-free.** Founder call (amending the blanket no-streaks stance): each PAIR of friends keeps a flame that lights when both do a pair action the same day, climbs per consecutive mutual day, and *sleeps* on a miss — the count banks to "longest flame," nothing is destroyed, and the UI never says who missed (the fire rests; no pig is blamed). Shown as staged hand-drawn art (spark → flame → bonfire → hearthlight), living on the friend rows; milestones pay BOTH art stages and matching worn cosmetics at day 30/90 (earn-only, pair-bound). Hard anti-goals: no lapse warnings, no timers, no paid freezes, celebration pushes only. Serves **Connect** (the dyad ritual — Snapchat's engine without its guilt) + **Collect** (matching trophies). UI brief: `docs/design/pair-flames-ui.md`; ships in wedge-plan Phase 2.

- **2026-07-15 — Bonds between two pigs become visible, ranked, and sendable.** Founder calls: (1) **Pair Keepsakes** — every pair action (fulfilled trades, blessings, barn visits; curses excluded — mischief isn't bond) accrues to a lifetime `pair_bonds` ledger per unordered pair, surfaced as a quiet keepsake line on the friend view. (2) **The Strongest Pairs board** — a global leaderboard of pairs by total bond ("who has done the most, any pair action"), champion treatment for #1, your best pair pinned. Pointedly NOT streaks: no decay, no guilt, pure accumulation. (3) **Rosie ships as an iMessage sticker pack** — a codeless Messages extension in the app so the herd can send Rosie in any group chat (real Unicode emoji is impossible; stickers are the honest equivalent). Serves **Connect** (friendship made visible and expressible, in-game and in the group chat) + **Contend** (a kind leaderboard) + **Collect** (keepsake milestones can pay pair cosmetics later). Migration `2026074300….sql` (authored, unpushed).

- **2026-07-14 — Open Sounders are knocked on, never walked into.** Founder call: a crewless pig can't force its way into an open Sounder — it *asks* (`request_to_join`, max 3 pending knocks, requests never reserve seats), and any current member opens the door (`accept_join_request`, cap-checked at accept) or quietly declines (no announcement to the asker — dignity). Crew→pig invites keep instant accept: that consent already flows both ways. The legacy `join_crew` becomes a transitional shim (creates a request, `{ok:true, requested:true}`) so pre-1.3 clients can't walk in either. Serves **Connect** — a herd is four pigs who *chose* each other; the door is warm, but it's theirs. Migration `20260742000000_join_requests.sql` (authored, unpushed — renumbered from 20260740000000 when the push-delivery hotfix 20260741000000 shipped first; it must go live together with the 1.3 knock-UI binary, since pre-knock clients have no way to see or accept requests).

- **2026-07-14 — Renaming belongs to pigs, not herds; leavers get a dismissible call back to the herd.** Founder calls on the 1.3 funnel: (1) **Username rename is the only rename** — one free, then it costs more each time (1,000 → 10,000 snouts, server-priced); its entry point is a quiet settings row on the scrapbook page (beside "What's new"), not a pencil shouting from the identity card. (2) **Sounder renaming is out for now** — the leader rename affordance is removed from the client (the server RPC stays dormant); a herd's name is its identity and identity churn is Connect-noise. (3) **Leaving a Sounder regresses the path to the join step** with leaver-specific encouragement — persistent until the player dismisses it (dismissal is allowed; nagging isn't) — and the join spotlight cuts its hole around **just the join button**, not the whole card. Serves **Connect** (the game always points a herdless pig back toward a herd, gently) + **Collect** (renames as a proper snout sink).

- **2026-07-13 — The Season-1 finale reward is title + exclusive cosmetic for every digger with ≥10 finds.** When the herd starves the Great Hungerer to Famished (or the season ends, whichever comes first — the grant is fired manually by the admin), every player whose Season-1 credited finds ≥ 10 receives the Hungerer's Crown (an exclusive hat, taken as a trophy — the lore already says "Hungry, his crown sits crooked", so at Famished the herd takes it) + the "Starver of the Hunger" title. Serves **Contend** — it's "I was there" prestige for the whole season-long co-op grind, earned-only (participation-gated at ≥10 finds), and mints zero currency so it can't inflate the economy. Grant RPC `grant_season1_finale()` is SECURITY DEFINER, revoked from authenticated (service-role/SQL-console only — dodges the `admin_only`-RAISE silent-rollback footgun), idempotent, and inlines its own `system_announcements` INSERT. Migration `20260739100000_season1_finale_reward.sql` (authored, unpushed).

- **2026-07-11 — Redemption codes ("Golden Tickets") grant, never sell.** Event/booth QR codes redeem to a hat, golden truffles, or snouts through one server RPC (`redeem_code`) — `max_uses` + per-user-once (`redemption_claims` PK) + ~39-bit Crockford-coded entropy + RLS-with-zero-policies (clients can never enumerate the codes table), all guards server-side. Truffles ride the existing `mint_truffles` ledger (999-cap-clamped, `war_truffles` audit row); snouts use the bounty `counter` idiom; hats reuse the `user_hats` grant. One legible loop: scan-or-type → named reveal → done (no inventory, no multi-grant bundles). Serves **Collect** (fair-by-construction: server-side, ledgered, per-user-once, cap-respecting, refusal-enveloped) — status stays earned/gifted, never bought. Migration `20260732000000_qr_redemption.sql` (authored, unpushed).

- **2026-07-11 — The season-flip login liturgy is canonical: old business → thank-you → new story → what you missed → the one ask → (next session) housekeeping.** Concretely, by popup-queue priority: Schism/Judgement verdicts (if pending) → Season-End recap + Founding Herd gift → **Great Hunger intro (newly queue-integrated at ~27, on the main page — it was a raw Modal that could co-present over the recap)** → While-Away → Sounder nudge; achievements + release notes are suppressed on any login where a ceremony fired and surface next session. Serves legibility ("one moment at a time") + Connect (the Sounder ask lands right after the story explains why herds exist). Ceremony beats housekeeping.

- **2026-07-11 — The Truffle Patch becomes a full fossil-dig loop: choose-what-you-get boards, UNIQUES, and the Burrow Book.** Founder session decisions: (1) **boards are no longer fully clearable** — depth tuned so ~60–70% of the mud is diggable per session; every dig is a choice, every session usually leaves a story behind. (2) **Uniques** — a Season 1 pool of 10–12 one-tile relics; the server rolls whether a board carries one (~2 in 5) and which (rarity-weighted); silhouettes are deliberately mysterious (no telling truffle from unique until you commit). First catch lights the relic's entry in **the Burrow Book** (a Pokédex-style collection page); dupes bump a found-count and pay extra meter drain. Set completion will pay the season's one dig-exclusive cosmetic. (3) **The One That Got Away** (next pass): a half-dug find returns gilded — presence grows on the board, value grows as catalog tier, never as minted currency (kills the leave-one-behind farming exploit); personal first, then single-object crew echo (a miss is ONE object that hops patches — never duplicated, never silently consumed). Shards rejected (currency in costume, gacha-adjacent, redundant with the Exchange); motes stay ambient plumbing. Serves **Collect** (the Book is proof-you-were-there, earned only) + **Contend** (uniques drain the meter; choice pressure makes the co-op dig matter) + **Connect** (crew echo, later). Art: uniques sprites via the new Codex/ImageGen pipeline.

- **2026-07-07 (night) — Rankings go season-cumulative with weekly spoils; the season tab's Sounder section collapses to one card.** Founder call after seeing the shipped sprint: the headline board is **total herd finds, accumulating all season** (every find counts forever, resets with the season) with the pinned-row UI; the **weekly race** (now Monday-anchored, 7 days — was Mon/Thu) survives underneath as the spoils engine — per-snout + quorum keeps Mondays fair and winnable by small herds, paying the truffle table + podium art. UI: the stepper, herd-presence card, and milestones card merge into **one Sounder card** — your actual pigs (presence lit on the roster), the dig button with the live cooldown, a one-line milestone strip — with the game's explanation moving into the season guide dialog (and "leave your Sounder" tucked there too). Serves **Connect** (the card is literally your herd's faces) + **Contend** (one number that always grows — the cozy read of ranking — plus a weekly beat anyone can take) + legibility (three stacked cards become one; explanation on demand, not furniture). Amends the charter's sprint language: accumulation within a season is in; standings never outlive one. Migration `20260720000000_weekly_race_season_board.sql`.

- **2026-07-07 — Charter rewritten to match the shipped game; the old charter + log retired to git history.** As of tonight the game is: the cozy core (Rosie, barn, friends, blessings, trades, visits) + Season 1's Great Hunger arc (crew-gated Truffle Patch digs every 8h feeding; finds mint Golden Truffles, drain the Hungerer, and build herd milestones) + **the Dig-Off Race** (every Sounder vs every Sounder, Mon/Thu cycles, finds-per-digging-snout, quorum 2, rank-scaled truffle spoils + podium art, pinned-row standings, push-driven) + the earn-only economy (Exchange, pass, achievements, titles). Specs: `docs/season1-coop-dig-spec.md`, `docs/digoff-race-spec.md`. Open gate: `race_podium_queue` needs its first art batch before real players hit a race end.

- **2026-07-13 — Leaving a Sounder never claws back what you dug for it.** A departed member's finds stay in the crew's weekly tally, lifetime count, milestones, and quorum (removing them would shrink the herd's earnings through no fault of its own — the no-shame rule). The *ledger* tells the membership truth instead: departed diggers' rows stay (the finds are real) but dimmed, sorted after current members, captioned "trotted on" — a pig never *appears* to be in two Sounders. Digs made after joining a new crew belong to the new crew. Serves **Connect** (leaving is safe, herds keep their story) and **Contend** (tallies stay honest). Live case: sivleg's 4 finds for The Truffle Barons after moving to The Bramble Snouts. **The one downside of leaving (confirmed, by design): you forfeit your claim to that crew's Monday spoils** — `_race_pay_cycle` pays current members only, and only those who dug for that crew this cycle, so leavers lose the credit and Sunday-night joiners can't freeload. Everything personal (XP, titles, finale finds) travels with the pig. `20260739200000_race_ledger_departed.sql`.

- **2026-07-23 — The weekly Dig Off ranks by overall finds; per-snout demotes to a lens.** Founder call (wayfinder map #38, ticket #50), reversing the 2026-07-07 per-snout fairness design: the week's winner is the herd with the most total finds — "most finds wins" reads at a glance, and full participation *should* win a herd race; the 4-member Sounder cap bounds the big-herd edge. Per-snout survives as the board's secondary metric toggle. Quorum revisit rides the migration (per-snout anti-gaming was its reason to exist; sub-quorum shame state likely retires). Same walkthrough batch: prize-hat cosmetic payout removed from the podium (truffles + tickles only, both now advertised on stacked podium cells); the last-race Ceremony gains its timeframe and a personal two-currency receipt. Serves **Contend** (a race everyone can read) + **Connect** (every snout's find visibly moves the herd). Specs on the map's tickets; implementation pending.


- **2026-09-10 — Barn rewards should lead naturally into expression.** Permanent Wallow gifts receive a durable, batched reveal; Later keeps New intact, and Preview changes only a draft until Save. Prestige-exclusive framed shelf keepsakes at ranks 1/3/6/10 carry the current rank inscription. Two named saved rooms separate collecting a look from making it public. Visitors can inspect and wishlist public designs; existing guestbook appreciation stays in the room. Serves **Collect** (clear provenance and earned identity) and **Connect** (a recognizable saved home), without a new currency, refund rule, or public voting system. See `docs/handoffs/2026-09-10-barn-completion.md`.

- **2026-09-11 — The barn is the door, not a button.** The Interior's single exterior entry is the barn building itself, standing on Rosie's ground plane (bottom-left, behind her, on every background); the gold "Enter Barn" button and the hidden silhouette retire. This supersedes the 2026-09-08 ruling's first clause ("Home leads with an illustrated Enter Barn button"): the room's visible Home / Furnishings / Decorate labels stand, but the way *in* is the building, because a button is a control and a barn is the game. It *beckons* (doors ajar, lamp glow) when the acquisition journal holds something New, so the thing you collect for is the thing that calls you home. Placement + motion plan: `docs/barn-entrance-plan.md`. Serves **Connect** (the home friends visit is a place you tap, not a menu item) and **Collect** (new furnishings pull you inside through the building, not a badge).

- **2026-09-12 — The Barn stops nagging; the dig is a door you open.** The "Updates · Truffle Patch" tray (active-effect chips + the "try a dig" nudge) is deleted from the app, along with the chips strip and the Hoofprints sheet that only it opened. In its place a single quiet button in the scene's bottom-right pulls up the Truffle Patch in place for crewed pigs and opens the Season tab for the uncrewed. Blessings and curses keep their full read in the Friends-tab Inbox. Serves **Contend** (the race's action is one tap from home, every time, without a banner asking for it) and **craft** (the home screen is Rosie, her barn, two tickets — not a notice board).

- **2026-09-12 — A blessing is one tap where the friend already is.** Rituals are free, capped at three a day, one per friend a day, and there is exactly one blessing and one curse each day — so the Bless/Curse segment and Cast button inside the profile sheet were ceremony, four taps from Home. Now the friend row carries one-tap **bless** and **curse** doors beside its visit door (the curse door arms on the first tap and casts on the second, because a curse has no undo), a strip above the list names today's two rituals and what is left, received blessings in the Inbox carry a one-tap **bless back**, and the profile sheet opens on Bless for friends. Serves **Connect** (the daily kindness is as easy as the daily visit, and reciprocity is one tap) and honesty about the economy (nothing to choose means nothing to confirm).

- **2026-09-12 — A visit is the friend's barn, not a dashboard about it.** The visiting screen drops from ≈208pt of header to ≈143pt: one plaque with the host's name (the only place it appears), one status row of avatar tags for hearts and visits, one Outside/Inside toggle floating on the scene, the visitor's "you" tag only, and one bottom pill that reads *Leave a hoofprint* then *Head home*. Tired is a toast; Leave never detours; "visits" is the one word for the counter. Plan: `docs/design/2026-09-12-barn-visit-simplify-plan.md`. Serves **Connect** (the barn and the pig fill the screen; the warm acts are one tap) and **craft** (say the name once, count the thing once).

- **2026-09-12 — The Barn guestbook is retired.** Stamps, the home placard, the interior dock, the in-visit "Leave a hoofprint" slot, and the kindness-card blessing that rode on a stamped visit are all gone (client + `20260913020000_retire_barn_guestbook.sql`). It was a second readout of a Connect act the visit itself already carries — the tickle, the shared hearts, the porch round — and on the home screen it was one more sticker between the player and Rosie. Existing Guestbook Keepsake owners keep the furnishing (`active=false`: hidden unless owned, never granted again). Supersedes the 2026-07-27 signing decision. Serves **Connect** by keeping the visit's one warm loop uncluttered, and the Barn's craft standard (the home is a place, not a notice board).

- **2026-09-13 — The Barn (Home) is Rosie's stage; everything else is two corners and one button.** The home keeps four things over the painting: the **earned stamp** (top-left, a rose heart with the lifetime total — the permanent number), the **coin** (tickle bank, top-right — the live number, with the streak on its shoulder and the regen clock as one hand line under it; a live ribbon hangs under that line as one small tag), the **Barn button** (bottom-right; its face is the default action — the shovel while the patch is open, the barn door otherwise — and a tap fans Dig / Go in / Bury a truffle), and Rosie. The barn sprite leaves the background, the ticket pair and the notice column are retired, and a ready weekly bounty is the Season tab's to announce, not the home's. Comp: `docs/design/claude-design/barn/barn-home.html` (the two-corner layout, E in `coin.html`, chosen over the one-strip F on 2026-09-13: earned and to-spend answer each other across the sky with Rosie between them, which is roughly the game). Serves **Collect** (the loop is tickle → bank, with nothing between the player and the pig) and **Contend** (Dig becomes the default the moment the patch opens).

- **2026-09-13 — One tap on the Barn button does the obvious thing.** The face IS the action: tap the door and you go in, tap the shovel and you dig. The `+` on its shoulder (or a long press) fans the rest — Dig / Go in / Bury a truffle — over a scrim, default first and biggest. Chosen over "always fan" because the default action is what the player wants nine times in ten, and a menu in front of it is a tax on the loop. Shipped in `components/BarnButton.tsx`; the go-in motion (`door-motion.html`) stays open. Serves **Collect** and **Contend** (the right verb is always one tap away).

- **2026-09-13 — The Barn button's face is a sticky quick action: chosen from the fan, fired from the page.** Supersedes the entry above's "the fan launches". The face wears one action — the door, the shovel, or the truffle — and a tap on it fires that action. The `+` (or a long press) fans Dig · Barn · Bury a truffle over a scrim, the armed one first and biggest; a tap in the fan never fires, it **arms**: the fan folds, the face flips, and the player fires from the page. The pick sticks per installation across taps, tabs and launches; before any pick the button arms its own default (Dig while the patch is open, Barn otherwise), and once the player has picked the patch opening no longer steals the seat — the dashed ring still breathes to announce it. "Go in" becomes "Barn". Chosen because the fan is a switch, not a launcher: set once, tap many times, and the verb on the face is the player's, not ours. Shipped in `components/BarnButton.tsx` + `components/Barn.tsx`; full spec in `docs/design/barn-home-brief.md`. Serves **Collect** and **Contend** (the right verb is one tap away — and stays there).

- **2026-09-13 — The buried truffle is folded by default; a tap says how many snouts are down.** The always-on "one buried" tag under the yard mound is gone: the steady state is the mound alone, a tap unfolds a tag reading "N snouts buried · check on it ›", and only the tag opens the buried-truffle sheet. The count is also on the Barn button's fan as an action item ("Your truffle · N snouts buried"). Chosen so the yard stays a painting until asked and the number arrives on demand rather than as standing chrome. Shipped in `components/BuriedMound.tsx`; spec in `docs/design/barn-home-brief.md`. Serves **Connect** (the friend's truffle is a thing in the yard, not a widget) and **craft**.

- **2026-09-13 — Everything you dig up pays tickles, and the receipt is a tally that ticks them in.** Every Snout Deep find is worth a number of tickles applied to the player's total count (banded by layer: topsoil 3–10, the mud 8–15, the root 12–25; a truffle he took pays nothing), on top of whatever the thing also does. The dig ends on a tally: *38 before → 105 tickled now*, then one ledger row per find landing in turn and rolling the number, *the dig · +67*, Back to the Barn. When he woke, the same tally with the taken truffle reading *his* — smaller, never zero. Chosen over a tumble-out on the Barn, a weigh-in scale, a stamp, and five variants of the tally because the count-up is the reward and the ledger already exists. Spec `docs/dig-redesign/a-snout-deep-spec.md` §2 + §5.7/5.8; design sources `docs/design/claude-design/dig/src/`; painted marks `assets/images/glyphs/dig/`. Serves **Collect** (the dig feeds the one number the player is growing) and **Contend** (tickles are the race).

- **2026-09-14 — The friend row keeps one “…” and one anchored actions panel.** Two rulings in one day: the morning's action-bar card (Visit · Bless · Curse · Profile always out under every friend, chosen from three directions on the design canvas) was built and green, then judged too tall once the row was seen against build 179's flat ledger. Final call: the row is the 179 shape — pig, name, one meta line, the herd line, a pin mark when pinned, row tap = profile — with a single **“…” trigger** on the rail that slides one **actions panel** over the row (Visit · Bless · Curse · Pin · Profile, icon over label). One panel open at a time; tapping another row's dots switches; an outside touch, a scroll, a reload or leaving the tab closes it; the curse still arms on the first tap. The panel is absolute inside a fixed-height row, so the list never shifts, and its geometry is asserted in a test so five 44pt cells fit at 375pt. Spec `docs/design/2026-09-14-friend-row-actions-menu-spec.md`, demo `docs/design/demos/friend-row-actions-menu-demo.html`, canvas claude.ai/code/artifact/9e0cc762-18de-4dff-adf3-d7e5678d54e0. The Porch Round scrapbook card leaves the Friends hub in the same pass. Serves **Connect** (every warm act on a friend is one tap behind one mark, on a list you can read at a glance) and **craft** (one row shape, nothing hidden that changes the page's height).

- **2026-09-14 — Rituals become a weekday of cosmetic effects, and stop touching play.** Seven blessings and seven curses replace both the Season 0 and Season 1 sets outright; the ISO weekday of the UTC date picks the day's pair (Monday is always Cloud Nine and Pickle Brine, Friday is always Golden Hour and Bacon Bits), so days earn reputations and "it's Bacon Friday" is a text message. No ritual changes regen, luck, tickles, or snouts any more; each is something you see on your pig or your Barn for six hours, and a day's pair never fights for the same visual channel. Chosen over a Season 2 set behind the season switch because a rotation that branches on a flag has already bitten once (the S1 mirror), and over the drifting day-of-year cycle because four does not divide seven. Plan `docs/design/2026-09-14-weekday-rituals-plan.md`. Serves **Connect** (a curse is a visible message from a friend, and visitors see it) and **legibility** (one sentence: every day has one blessing and one curse, and you wear them).

- **2026-09-14 — Satchel finds are given, never sold or paid for.** A Dig drops small finds into a capped bag; every pig carries one rolled wish; a visitor who hands a friend's pig the find it is hoping for tickles both pigs the same flat amount and keeps a count, a generous tick and a keepsake at 10 / 50 / 100 — never Snouts, never Golden Truffles, never a currency of any kind, and rarity lives in the catalog, not the payout. Rolled wishes (with one owner "not this one"), not a picker; Dig as the only source, not a rummage on the visit screen; no per-day caps because there is nothing to farm. In the same build the visit's rules were set: **the host's pig is the only tickle target**, the two pigs **face each other**, and "tickled out" is a **tag under the host pig**, not a toast. Serves **Connect** (a reason to visit a *specific* friend, and to be the friend whose pig wants something); **Collect** owns rarity; the delivery count is the **Contend** slice. Spec `docs/satchel-spec.md`, `docs/barn-visit-facing-spec.md`.

- **2026-09-16 — Finds swap in the barn; the shelf folds into the bag; the Stranger waits.** A visitor who hands a friend's pig the find it is hoping for may take one of up to three finds the host's bag can spare — chosen by the server from the host's biggest stacks so a bag is never on show — or just give it. Finds move 1:1 between bags and mint nothing; the flat 3 tickles a delivery already paid stay, capped per pair and per pig per day; a generous tick on gifts only; one swap per pair per UTC day, enforced on the server. A received find is in your bag — tossable, giveable on, counted toward the catalog — and the shelf retires. "Given, never sold" stands unchanged. Founder ruling: **finds + swaps ship first; the Stranger / Trading Hut bundle (`docs/design/2026-09-16-finds-swaps-and-the-stranger.md`, which amends the 09-14 ruling) comes later and is not ratified by this entry.** Serves **Connect** (a specific reason to visit a specific friend), **Collect** (a swap is how a lone digger meets a rare), **Contend** ("N swapped" on the Board). Spec `docs/design/2026-09-16-satchel-audit-and-barn-trading-plan.md` Part 2 + §12 wire contract; migration `20260917100000_satchel_swaps.sql` (authored, unpushed).

- **2026-09-15 — A pig that faces a friend turns; the room owns the pigs' size; the base room is empty.** Three rulings from one screenshot of the Inside visit. (1) The mirror-only facing of 2026-09-14 put two front-facing pigs "at" each other; now every pig has a **three-quarter turn** — `face` standing, `face_sit` seated, four frames each (rest, breath in, blink, breath out) from one Codex ImageGen sheet per pig — and `PigStage facing` means which way the pig looks: the guest right, the host left (mirrored). Moods and reactions keep their front frames. The 2026-09-14 note that a turn "would break every RelSpec anchor" is answered with per-family anchors, and a hat keeps its size on the turn (only eye-bound items foreshorten). (2) The visit's `TapPig` pre-shrank each pig (0.62 / 0.54) and the room scaled the 300pt canvas again (0.36 / 0.30 of its width): a 60pt pig on a 390pt room. The room is now the one owner of pig size (`PIG_SPOTS`: host 0.44, guest 0.36, on either side of the middle; the owner alone stays just off centre) and the room's TapPig is handed the canvas at 1:1. (3) `warm_plank_barn` is repainted **empty** — no shelf, no wardrobe — so the room is a stage for the pigs and the player's own decor, not a furnished set they decorate around; the same afternoon Spring Whitewash and Midnight Rafters were repainted empty too, and the "Shelf" decorating spot — which sat on the painted shelf — now draws its own plank (`shelf_plank.png`, chrome) under whatever is set on it and while decorating, so every object in a room is the player's. (4) The guest stays `content` for the whole visit: the happy mood after the first heart played front frames and turned the guest back to the camera, undoing the scene on its first tap; the hearts are the floats. Serves **Connect** (a visit is two pigs meeting, big enough to read) and **craft** (a base that shows its owner's choices, not ours).
- **2026-09-15 — The blessing/curse cast moment is a shared bubble.** A landed cast from a friend-row door or the Inbox bless-back rises the ritual's own art in a bubble from the bottom of the screen to the header and pops; the success toast that told the bless-back's outcome is gone, the row's notice remains the record, refusals keep the toast, and the profile sheet's picker keeps its own sent beat (native Modal). Serves **Connect** — a send is felt leaving your hands, not read as a receipt. Craft rulings in `docs/design/taste-standard.md`.
